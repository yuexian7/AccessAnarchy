# Access Anarchy 开发笔记（调研结论 + 实现记录）

- **生成时间**：2026-09-25（**v0.8.0 已发布 Paradox Mods**：平台版本 3 → 4，`userModVersion` 0.8.0，见「发布记录（v0.8.0）」；发布同轮还做了三件事——① 模组游戏内名称 12 语本地化，中文＝**出入口无碰撞**、平台名保持 Access Anarchy（§⑥）；② 查清「每次重启弹快捷键重复」＝R/T 撞原版相机 Zoom/Rotate，且冲突动作会被游戏直接禁用（§⑤）；③ 新增 `Key binding check` 键位诊断日志（§⑦）。⚠️ 本轮**线上 API 回读被权限策略拦下、未实测**，只有工具链成功回显为证。上一轮 2026-09-24（v0.8.0 本地构建；三条硬结论先记：① `LaneOverlapSystem` 全文**从不**给车道 `AddComponent<Updated>`，所以"重建后车道还带 Updated、我们当帧重删"这条自 v0.7.4 起的假设是错的——出入口仍会避让的根因；② 带 `[SettingsUIDisableByCondition]` 的属性不加 `[SettingsUIForceSave] 就**不落盘**，三个出入口范围勾选框从来没被保存过；③ 官方模板的键位顺序是 `RegisterKeyBindings()` **先于** `LoadSettings()`，本模组反了，导致 `[AfterDecode] ApplyKeyBindings()` 永不执行、盘上的空 `m_Path` 被原样注册 → 重启后 F3/F4 失效）；上一版 2026-09-20（**v0.7.9 已发布 Paradox Mods**，代码零改动、只换封面 + 写日志，见「发布记录（v0.7.9）」；⚠️ 该节两条硬结论：① `<Thumbnail>` 就是标题旁的方形封面、每次发版都会被全量重推，别让它指回 ThumbGen 占位图；② 线上 API 走 Varnish、刚发完新旧副本交替返回，验证要循环打十几次而不是看一次）；2026-09-19（**v0.7.8 已发布**，出入口模式"关不掉"修复见 §0.7⑦）
- **游戏版本**：1.6.0f1（Unity 2022.3.71f1 构建，工具链 Entities 1.3.10）
- **调研方法**：`ilspycmd`（9.0.0.7889）反编译 `F:\SteamLibrary\steamapps\common\Cities Skylines II\Cities2_Data\Managed\Game.dll`，全文反编译副本在 `research/decompiled/`（2333 个文件，个别复杂类型反编译失败缺失，用 `-t <全名>` 单独反编译补齐；`research/search.mjs` 是源码搜索小工具——注意 findstr 的 `\|` 不是交替语法、`<>` 会被 cmd 重定向吞掉，用 node 搜）。
- **自足性**：新会话只读本文件 + 源码即可继续迭代。所有结论都给了反编译证据位置，查无实据的写成「未验证」。

---

## v0.8.0（2026-09-24 开发，2026-09-25 已发布 Paradox Mods，见「发布记录（v0.8.0）」）

用户这一轮提了三件事：① 个别停车场出入口仍会避让（同一个停车场早前测是好的）；② 选项界面最下方加「关于」板块（版本 / 作者 / 三个跳转按钮）；③ 核实模组设置与快捷键重启后是否加载上次退出时的值，并保证与 SIMPLE MOD CHECKER PLUS 的备份还原不冲突。三件都有反编译或本机磁盘实据，结论如下。

### ①出入口仍会避让：`Updated` 事件信号根本不成立（§0.7 那条早期假设被证伪）

- **反编译证据（`research/decompiled/Game/Net/LaneOverlapSystem.cs`）**：全文 `Updated` 只出现在 5 处，**全部是只读**——`:29` `AddNonUpdatedEdgesJob.m_UpdatedData`、`:975` `UpdateLaneOverlapsJob.m_UpdatedData`、`:1083/:1087` 跨拥有者取节点时判 `m_UpdatedData.HasComponent(edge.m_Start/m_End)`、`:1893-1894` 两个查询定义。**没有任何一处 `AddComponent<Updated>`**。它重建 overlap 是按 owner 的 `SubLane` 缓冲逐条 `bufferData.Clear()`（`:1077`）再 `Add`（`:1400`/`:1420`），被改写的车道不会因此带 Updated。
- **车道级 Updated 的真实来源**：`Game/Net/LaneSystem.cs:2092`（owner 的车道整批重生成时 `AddComponent<Updated>(laneBuffer.AsArray())`）、`Game/Buildings/RoadConnectionSystem.cs:1321`（新建连接道）、`CityConfigurationSystem.cs:231`（左右行/主题切换）、`RequiredComponentSystem.cs:1203`（老档迁移）。都是"车道被重建"，不是"overlap 被改写"。
- **所以漏口是**：任何一次 owner（建筑/节点/edge）被 Updated 而车道没被 Updated 的改写——停车场/建筑状态变化、出入口相邻节点被别的编辑牵动——行人条目回来后 `m_RefreshQuery` 收不到，只能等集合整轮重建（`kSetRebuildInterval=2048` 帧 ≈ 34 秒）时 `StripAccessOverlapsFromSetJob` 兜回来。**"早先是好的"= 那一刻离上次整轮重建近；现在能看见避让 = 刚发生过一次 owner 级重建**。全局模式更糟：它连周期性重删都没有（`m_InitialGlobalApplyDone` 置位后只走 Updated 事件），条目一旦回来就永远回来。
- **修法（新增干预点 (0) `StripRebuiltLanesJob`）**：闸门与原版逐条同口径 `m_RebuildOwnerQuery = All[SubLane,Updated] None[Deleted]`（≡ `:1893`），遍历对象也与原版工作清单一致（owner 的 `SubLane` + `AddNonUpdatedEdgesJob` 那批"自身未 Updated 的相邻 edge"，`:26-54`，用 `ConnectedEdge.m_Edge` 取）。
  - 时间可行性核对过（这是上一轮翻车的教训，先证明再看代码）：`SystemUpdatePhase` 枚举顺序 `Modification4B(8) < GameSimulation(13) < Cleanup(31)`；`LaneOverlapSystem` 在 Modification4B（`Game/Common/SystemOrder.cs:178`），本系统在 GameSimulation；`Updated` 的移除点是 `CleanUpSystem.cs:53`（`SystemOrder.cs:54`，phase Cleanup），`PrepareCleanUpSystem`（`SystemOrder.cs:50`，MainLoop）只是 `ToEntityListAsync` 抄一份清单给 CleanUpSystem、**不移除**。⇒ 同一个非空状态我们在同一帧看得见，一帧内收敛。
  - **不会再变成每帧写战**：`pedestrian == 0` 直接返回，不调 `SetBuffer`；只有原版真的把行人条目写回来的车道才产生 ECB 写入。这是 v0.7.2 删掉干预点 C 之后确立的原则（内容判据代替无条件重写）。
- **顺带修**：判定侧 `IsNearAnchor(bezier)` 原来只取 `a / d / 弦中点` 3 个采样，而放锚点用 5 个点。长或弯的引道"弦中点"明显偏离曲线本体，出入口落在偏离段上时这条车道既进不了集合也过不了逐帧锚点判定 → 永久漏删。现在两侧统一为 5 点。
- **未修、先测量（避免又一次盲修）**：`AddAnchor` 的 `list.Length >= 14` 是 `FixedList128Bytes<float2>` 的硬容量（128 B ÷ 8 B = 16，留长度前缀 → 14），**不是可调参数**。大停车场 AreaLane 密布时一个 32×32 米格子超 14 个采样点是会发生的，被丢掉的锚点会让邻近车道判不出"靠出入口"。本轮只加计数器把它测出来（心跳日志 `Anchor grid capacity hit`），拿到实机数据再决定换 CSR / MultiHashMap，不在没证据时改判定半径。
- **三行新自证日志（心跳 ~30 秒一次，`ReportRebuildDiagnostics`）**：
  - `Rebuild gate: N frame(s) with vanilla LaneOverlap rebuild ... re-stripped X lane(s) / Y pedestrian entries` —— 闸门开合次数与我们跟上的量（64 槽计数，属下界）。
  - `Anchor grid capacity hit: N anchor point(s) dropped ...` —— 只在真有丢弃时出现（WARN）。
  - `Access set audit: A of B vehicle lanes still hold C pedestrian overlap entries` —— 集合内此刻残留多少行人条目（出入口模式恒排除横道条目，横道我们本来不动）。
  - **判读**：闸门非 0 而 audit 也非 0 = 我们没跟全，漏口还在；闸门 0 且 audit 0 却仍然避让 = 不是 `LaneOverlap` 这条通道（行人就站在车道本体上走 `CheckCurrentLane`，或行人静止被 `VehicleCollisionIterator` 当静态障碍，两者都是本模组保留的原版行为）。

### ②「关于」板块：框架原生支持，不需要自定义面板

- 查证结论：`Game.UI.Menu.AutomaticSettings` 的 `WidgetType` 里就有按钮，**不用**写 `SectionPanel` 之类的自定义面板（顺便记一笔：`SectionPanel`/`GroupPanel`/`CheckboxOptionPanel`/`IUIPanel` 这些类型在 Game.dll 里**不存在**，`research/alltypes.txt` 0 命中，别再按别的模组教程里的老名字找）。
- 版本/作者两行 = **只读 `string` 属性**（get-only → `WidgetType.StringField` → `Game.UI.Widgets.LocalizedValueField`，其 setter 是空委托）。原版先例 `Game/Settings/About.cs:21` `public string gameVersion => Version.current.fullVersion;`。
- 三个按钮 = **只写不读的 `bool` 属性**，同一 `[SettingsUIButtonGroup]` 合并成一行。硬守卫在 `AutomaticSettings.AddBoolButtonProperty`：`if (canRead || !canWrite) return null;` —— 属性**必须真的没有 getter**，写成 `{ get; set; }` 再加 `[SettingsUIButton]` 会整行消失。原版先例 `Game/Settings/ModdingSettings.cs:37-101`（toolchainAction 四个按钮同一行）。
- 打开链接用 `UnityEngine.Application.OpenURL(string)` —— 游戏自己就是这么做的（`Game/UI/Menu/ParadoxBindings.cs:708`，`ShowLink` 里）。`UnityEngine.CoreModule` 已在 csproj 引用。Game.dll 里**没有**内置浏览器，也没有 `<a>` 链接渲染（描述文字里的 URL 不可点，所以必须做按钮）。
- 文案：`zh-HANS` 逐字用用户口述「模组版本 / 作者 / 请我喝杯咖啡 / 论坛页面 / RAINBOW官网」，其余 11 种语言各自翻译；`作者` 一行按用户原话是 `yuexian`（不是 yuexian7），逐字使用。
- 新增 `ModInfo.cs` 收敛版本号/作者/三个 URL：v0.7.9 出现过"线上是 0.7.9、启动日志仍写 v0.7.8"，就是版本号散在日志字符串里导致的。

### ③设置与快捷键持久化：两条都是真 bug，且都影响玩家

- **出入口范围三个勾选框从来没落盘**。本机 `AccessAnarchy.coc`（实测在 `...Cities Skylines II\AccessAnarchy.coc`，**不是** `ModsSettings\`——`[FileLocation]` 是文件名不是子目录）里只有 `ToggleEnabledBinding / ToggleScopeBinding / Enabled / Mode` 四项，三个 `Include*` 全都不在。原因：它们带 `[SettingsUIDisableByCondition]`，而原版同一处证据 `Game/Settings/GeneralSettings.cs:93-96` 的 `autoSaveInterval` 正是 `[SettingsUIForceSave]` + `[SettingsUIDisableByCondition]` 成对出现。⇒ 三个属性补 `[SettingsUIForceSave]`（`SettingsRevision` 这个隐藏项也一并加，它不在页面上）。
- **重启后 F3/F4 会失效**（此前被 SIMPLE MOD CHECKER PLUS 掩盖，所以作者没察觉）：
  - 官方模板 `template/content/Mod.cs` 的顺序是 **`RegisterKeyBindings()` 在前、`AssetDatabase.global.LoadSettings()` 在后**；本模组反过来，注释还写着"LoadSettings 必须先于 RegisterKeyBindings"——**是错的**。
  - 机制：框架读档后落地键位的钩子 `ModSetting.ApplyKeyBindings()` 带 `[AfterDecode]`，且被 `keyBindingRegistered` 闸门挡住（`Game/Modding/ModSetting.cs:269-277`），该标志要到 `RegisterKeyBindings()` 结尾才置 true（`:228`）。旧顺序下钩子从来没跑过，`RegisterKeyBindings` 直接拿盘上的 `m_Path` 去 `AddActions`（`:156` → `:208` → `InputManager.cs:2101` 原样应用 `binding.path`）。
  - 空串会被当成有效绑定应用：`CompositeInstance.m_CanBeEmpty = true`（`CompositeInstance.cs:12`）使 `InputManager.cs:997` 的守卫放行，`TrySetMainBinding` 把 `overridePath = ""` 写进去（`:1029-1034`）。之后退出时 watcher（`ModSetting.cs:213-216`）又把空值写回属性 → 落盘 → **自我延续**。本机证据：`"ToggleEnabledBinding": {"m_Path": ""}`、`"ToggleScopeBinding": {"m_Path": ""}`。
  - 修法：① 顺序改回官方模板那样；② `Setting.RepairLegacyKeyBindings()` 只把**路径为空**的键位回填成 `<Keyboard>/f3|f4`（`SettingsUIKeyboardBindingAttribute.cs:101-102` 的映射逐字），`SetBindings` 后 `TrySetMainBinding` 见 path == 建资产时的 path 会**清掉 overridePath**（`:1019-1027`）；玩家真改过的非空键一律不动。③ 用隐藏项 `SettingsRevision` 做一次性闸门，避免玩家以后主动清空键位被每次启动强行恢复。
  - `ProxyBinding` 是 **struct**（`Game/Input/ProxyBinding.cs:12`），改完必须经属性写回，否则只改到副本。
- **与 SIMPLE MOD CHECKER PLUS 的共存判断：不冲突，且它是"救命的那个"**。本机 `Logs\SimpleModCheckerPlus.log` 实测时序：mods 20:49:23 加载 → SMC+ 还原 GameSettings 20:49:26.35 → **Keybinds 20:49:26.47** → ModSettings 20:49:33.20，全部晚于本模组 OnLoad，所以它后写、它赢。本模组侧天然跟随：① `Enabled/Mode/Include*` 都是实时 getter，被外部改属性立刻生效；② 热键订阅在 **ProxyAction** 上（`onInteraction`）而不是绑定上，外部重新绑定不影响触发。我们的修复只动"空路径"这一种情况且跑在 OnLoad 里，不会回头覆盖它还原的键。
  - ⚠ **待办提醒用户**：升级 v0.8.0 后要在 SMC+ 里**重建一次备份**。旧备份存的是 0.7.9 时代被写坏的 `"Path": ""`（实测 `KeybindsBackup_prev.json` 里本模组两项都是空，而 `KeybindsBackup_1.json` 是玩家真正用的 `<Keyboard>/r`、`<Keyboard>/t`），继续拿旧备份还原会把空键位又带回来。
- **⚠ 本轮构建的部署路径**：这次构建才第一次把产物拷进 `...\Cities Skylines II\Mods\AccessAnarchy`。而 2026-09-24 那一局的日志第 2 行显示游戏当时加载的是 `F:\...\.cache\Mods\pdx_mods\158842_3\AccessAnarchy.dll`（**线上已发布的 0.7.9**）。实机验证第一件事就是看这行有没有变成 `Mods\AccessAnarchy\AccessAnarchy.dll`、第 1 行版本有没有变成 v0.8.0，否则测的还是旧版。

### ④第二轮（同日，用户实机反馈后）：三个勾选框本来完全无效 + 文案对齐官方术语

用户实测反馈："关掉任意一个都不影响模组功能"。查代码即证实，不需要实机：**锚点网格完全不看出关**——`m_AnchorQuery` 无条件收所有 `GarageLane|ConnectionLane|AreaLane` 的曲线，而出入口模式判据是「类型 / 集合成员 / 锚点邻域」三选一，所以即使按类型排除某一类，该类旁边的其它类锚点照样命中"锚点邻域" → 开关形同虚设。

- **三类重定义为互不重叠**（`Setting.kCatParking=1 / kCatBuildingAccess=2 / kCatBuildingInternal=4`，属性改名 `IncludeParkingLots / IncludeBuildingAccess / IncludeBuildingInternal`）：
  - 停车场与停车楼：`GarageLane`，或所属建筑带 `Game.Buildings.ParkingFacility` / `CarParkingFacility` 的 Connection/Area/Car/Parking 车道。
  - 建筑与外部道路的车辆出入口：**非停车场**建筑的 `ConnectionLane`。
  - 建筑内部道路：**非停车场**建筑的 `AreaLane` 与建筑自有 `CarLane`/`ParkingLane`。
- **"停车场 vs 其它建筑"只能看所属建筑，不能看车道组件**（这条是本轮最重要的新知）：停车场大门那条连接道和商店大门那条是同一个 `ConnectionLane`，flags 都是 `Inside|Road`（`Game/Buildings/RoadConnectionSystem.cs:1356-1363`）；`ConnectionLaneFlags.Parking` 标的是车位/自行车架那种 berth 链接（`:1370-1391`，自行车分支 :1255、`RouteConnectionType.Pedestrian` 分支 :1376-1391），停车场门口反而**没有**它。可靠判据：建筑出入车道的 `Owner.m_Owner` 直接就是建筑实体（`RoadConnectionSystem.cs:1332 new Owner(building)`），而 `ParkingFacility`/`CarParkingFacility` 只有停车场类建筑才有——反证在 `Game/Pathfind/ParkingLaneDataSystem.cs:405-418`：没有 `ParkingFacilityData` 的建筑会退化成用 `BuildingPropertyData.m_SpaceMultiplier`（住宅）或 `WorkplaceData.m_MaxWorkers/20`（工商）估容量。两个组件都是 `IComponentData`（`Game/Buildings/CarParkingFacility.cs:8`、`ParkingFacility.cs:6-10`），`ComponentLookup` 直接读，Burst 安全，不需要 PrefabCollection、不需要 guid 预烘表。
  - 已知残余：停车场若来自 `InstalledUpgrade`（`ParkingLaneDataSystem.cs:367-382` 会遍历升级），实例组件是否同步加上未实机验证；实机如发现某类建筑分错，第一嫌疑就是这里。
- **三份类型判定收敛成一处**：`LaneCategory()` + `IsInNoYieldScope()`（chunk 版只保留全局模式用的 `IsGlobalLaneByChunk()`）。此前 `IsAccessLaneByType` / `BuildAccessSetsJob.IsAccessType` / 新增那份各写一遍，是"进了集合却判不成范围内"这类漏口的温床。另外三个 job 里各自写死的 3 点 `IsNearAnchor` wrapper 已改为委托给系统级 5 点实现。
- **锚点网格与邻域集合都按 `IncludeMask` 过滤后收录**；掩码变化沿用既有 `narrowed` 分支（先让原版整表重建 + `kReapplyDelayFrames=120` 帧再按新范围重删），所以关掉某一类会在 1-2 秒内恢复该范围的避让，不留永久剥离态。
- 板块名 `出入口范围` → `不避让范围`（12 语言各自对应）。

**官方术语校对（这次有真数据，不是猜的）**：游戏官方文案在 `F:\SteamLibrary\steamapps\common\Cities Skylines II\Cities2_Data\Content\Game\Locale.cok` —— 是个 ZIP，内含 `de-DE.loc … zh-HANT.loc` 12 个文件；`.loc` 格式 = `[u8][u8]` + 3 个 varint 长度字符串（语言名/locale id/名）+ `u32 条数` + 每条 `(varint 长度 key, varint 长度 value)` UTF-8，en-US 实测 22163 条。解析脚本留在 `D:\WorkSpace\CS2MOD\AccessAnarchy\.local\locale\{extract.ps1,probe.cjs,q.cjs}`（`.local/` 未进 git，属本机草稿）。查证到的官方词：`Options.SCREEN_TITLE` zh-HANS=设置选项、ru=Параметры；`Options.SECTION[General]` zh-HANS=综合、zh-HANT=綜合、ru=Общее、ja=一般；`Options.GROUP[Input.Shortcuts]` de=Kurzbefehle、ru=Ярлыки、ko=단축키、zh-HANS=捷径，而 `Options.INPUT_MAP[Shortcuts]` zh-HANS=**快捷键**（同一个概念两个 key 用词不同，取 INPUT_MAP 那个，因为它才是按键页的词）；`Assets.NAME[Crosswalk]` zh-HANT=**斑馬線**（不是行人穿越道）；`Assets.NAME[Connection Lane]` zh=连接车道/連接車道、de=Verbindungsspur、ja=接続レーン、ru=Соединительная полоса；`Assets.NAME[ParkingHall01]` ja=立体駐車場、ko=지상 주차 빌딩、fr=Parking couvert、zh-HANS=地上停车场、zh-HANT=立體停車場；`Paradox.MOD_AUTHOR` zh-HANS=作者、ja=作成者、ko=제작자。
按这些改的部分：`tab`（zh-HANS 常规→综合、zh-HANT 一般→綜合、ru Общие→Общее）、`bindingMap`（de/ru/ko/ja/zh 两侧全部换成官方"快捷键"词）、热键说明里的"选项菜单"（zh→设置选项、de→Menü Optionen、ru→меню параметров）、`mode.global`（es vías→carreteras、fr voies→routes、ko 전역→전체）、`group.scope` 全部重写成"不避让/无优先"式说法。顺手修掉一个真错：德文 `mode.warning` 里的 `stadtwelt` 是错拼，应为 `stadtweit`。
**故意没改**：`避让` 没有整体替换成官方 `让行`。理由——① 官方语料里 `yield` 只出现在资产名（`Assets.NAME[EU Car Yield Line]` 等），设置页没有一个可对齐的"让行"术语；② `避让` 是作者自己定的说法，线上模组页中文描述、README、既有 12 语文案全都是它，换成"让行"反而和玩家已经看到的各处不一致。这条判断已写在这里，日后要改再改。

**已按作者要求撤掉的东西**：`RepairLegacyKeyBindings()`、隐藏项 `SettingsRevision`、以及"空键位自动回填 F3/F4"的整套逻辑。现在键位盘上是什么就是什么（空着就空着），只保留"先 `RegisterKeyBindings()` 后 `LoadSettings()`"这个顺序修复——它才是让玩家自己存的键真正生效的那一步。⚠ 副作用：v0.8.0 第一版已经在作者机器上跑过，那次迁移可能已把 `f3`/`f4` 写进 `AccessAnarchy.coc`，需要作者在选项页手动清一次（或删 .coc）才会回到"空"。

---

- **Paradox Mods**：ModId 158842 平台版本 2 → **3**，`userModVersion` **0.7.9**，size 148,271 → **148,272 B**（差 1 字节是 zip 时间戳之类元数据，**代码零改动**：本轮只动 `PublishConfiguration.xml` 与封面图，`git status` 里没有任何 `.cs`）。命令仍是 `dotnet publish -p:PublishProfile=PublishNewVersion -c Release`。
- **本轮只做两件事**（用户明确要求：不动模组任何代码、不更新 GitHub）：① 把标题旁的方形封面换回作者做的那张海报；② changelog 按用户口述原文写「修复了一点小问题」，中英各一条，**不夹带任何其它信息**。
- **封面为什么会被打回去（根因，下次别再犯）**：`FillPublishMetadata` 每次 NewVersion/Update 都会**全量重推 thumbnail**，取的是 XML `<Thumbnail Value="...">` 指向的本地文件。v0.7.8 那次它把 `Properties/Thumbnail.png`（`research/ThumbGen/` 自造的 950×500 占位图）推成了线上 `cover_2.jpg`，把用户 9-13 在网页端上传的方图海报盖掉了。**只要 XML 还指向 ThumbGen 产物，每次发版都会重演一次**。现已把 `<Thumbnail>` 改指 `Properties/Thumbnail.jpg`（作者原图 1254×1254，逐字节拷贝、不缩放不裁切不重编码），并把 `Properties/Thumbnail.png` 删除。
- **「缩略图必须 950×500」是错的（本次反编译证伪，§发布流程踩坑 #3 已就地改正）**：`ModPublisher.exe` 的 `PrepareThumbnail`（反编译 `/tmp/mpd/ModPublisher/Program.cs` L272-296）**只判断文件存不存在**，不存在就退回内嵌资源 `ModPublisher.Resources.Thumbnail.png`，全程没有任何尺寸/格式校验；SDK 侧 `ModsPublishingService.cs` L535 `publishRequest.thumbnail = "cover" + Path.GetExtension(...)`、L468 presigned URL 同样沿用原扩展名，**没有扩展名白名单**。950×500 只是 ThumbGen 自己产图的尺寸，不是平台要求。
- **平台侧封面行为（实测）**：上传的封面按内容落成 `cover_N.jpg`，平台会**重编码 + 按内容去重**——本次上传 1,323,134 B 的源图，线上 `cover_3.jpg` 是 362,213 B、与 9-13 那张 `cover_1.jpg` **逐字节相同**（同一张海报的规范化产物），`displayImagePath` 在缓存新旧两个副本里分别指 `cover_1.jpg` / `cover_3.jpg`，**两者都是那张海报**，所以无论命中哪个副本都不会再看到 ThumbGen 占位图。
- **线上回读（2026-09-20）**：`changelog` **2 条**（版本 2/0.7.8 旧条 + 版本 3/0.7.9 新条），新条正文 `v0.7.9\n\n*   Fixed a few minor issues.\n\n中文\n\n*   修复了一点小问题。`——**换行完整保留**，`- ` 被平台 Markdown 规范化成 `*   `（渲染仍是项目符号，属平台行为不是 bug）。2 张网页端截图仍在。订阅 589 → **635**。
- **回读会被 Varnish 双副本骗到（本次新坑，验证时务必注意）**：`https://api.paradox-interactive.com/mods?modId=158842&os=windows` 走 `via: 1.1 varnish`，刚发完有一段时间**新旧两个副本交替返回**（旧副本：`userModVersion=undefined`、changelog 1 条、cover_1；新副本：0.7.9、2 条、cover_3）。单次请求看到「还是旧的」不等于发布失败。正确做法：**循环打十几次**，只要出现过新副本即为成功（本次实测约十几秒内 12 次里能命中新副本）。网页端 CHANGELOG 区同理，会滞后到缓存过期。
- **ChangeLog 元素文本形式确认无误**：`UpdateOptions.cs` L35 `options.ChangeLog = item.Attribute("Value")?.Value ?? item.Value`，`XElement.Value` 对**纯文本内容**保留换行（只有属性值会被规范化成空格、只有子节点拼接才丢换行）。所以 §ChangeLog 机制 #2 的「元素文本 + 行首不缩进 + `- ` + 空行」写法是对的，本次一次通过。
- **潜在风险（未修，留此备忘）**：仓库没有 `.gitattributes` 且 `core.autocrlf=true`，`PublishConfiguration.xml` 在对象库里存的是 LF（HEAD blob 实测 0 个 CR），但**全新 clone 到 Windows 会检出成 CRLF**，届时 `<ChangeLog>` 元素文本里的换行就变成 `\r\n`。本次发布用的是工作区现有的 LF 文件，平台侧渲染正常；`\r\n` 大概率也没问题（Markdown 通用处理），但既然 changelog 格式已经翻过一次车，下次发版前顺手确认一下工作区文件仍是 LF（`grep -c $'\r' Properties/PublishConfiguration.xml` 应为 0）。
- **⚠️「Mod pending publication / 待发布」≠ 没发布成功（2026-09-20 用户在这慌了，实证记这里）**：`https://mods.paradoxplaza.com/uploaded`（作者"我的上传"列表）里模组卡片会挂 `Mod pending publication` 徽章，卡片根类名是 `Card-styles__state-publishing`。对照 SDK `EnumConversionExtensions.cs` L42-67 的 `statesKVs`：平台 `state` 取值是 `published` / **`publishing`** / `disabledByManager` / `removedByUser` / `rejectedVirus` / `autoBlocked`——`publishing` 是**上传后自动处理流水线中**（病毒扫描 / 审核 / 建索引）的正常中间态，不是"等你点发布"，也不是被拒。该卡片只提供 `Manage Collaborators` 和 `Delete mod`，**没有任何 Publish 按钮**，作者无事可做，等它自己翻成 `published` 即可。
  - 同时匿名接口 `mods?modId=158842&os=windows` 已经返回 `state:"published"`、`modVersion=3`、`latestVersion=3`、changelog 2 条、封面新图——**版本实际已建好并可分发**，作者列表的状态位只是另一套视图/索引还没跟上（同一张卡片上 size 还显示 126 KB = 0.6.0 的包大小，可佐证索引滞后）。
  - 判断"到底发上去没有"以这三条为准：`mods/versions?modId=158842` 里有该 userVersion、模组页 ALL VERSIONS 面板列出该版本、匿名 `mods?...` 的 `modVersion/latestVersion` 指向它。**不要拿 `/uploaded` 的徽章当发布失败**。
- **知识中心回写（2026-09-20，按 `D:\WorkSpace\CS2MOD\都市天际线2模组开发标准流程.md` 附 C 协议）**：本轮结论中**跨模组可复用**的 7 条已进 Playbook，正本版本 **v1.0 → v1.1**（工作区 md 与知识库同步）：① `<Thumbnail>` 引用的文件是发布物、删了会静默退回内嵌官方占位图；② `<ChangeLog>` 只能写当前版本；③ `ModState.Publishing` =「Mod pending publication」是处理中不是失败；④ `displayImagePath` 文件名不能当判据、平台按内容去重封面；⑤ `size_in_memory` 只配当"变了/没变"开关；⑥ Varnish 多副本机制 + 作者侧字段读不到时改用 DOM 类名+SDK 枚举推断；⑦ `.ps1` 中文注释吞语句第三次原样复现。附 B 硬规则 22 → **24 条**（新增 23/24 两条素材与状态判读规则）。
  - 知识库 **CS2MOD**（notebookId `01a06f99-c25b-75bd-bd00-47e98e2905aa`）新源：**「都市天际线2模组开发标准流程 v1.1（含踩坑与更优决策）」sourceId `01a0bd9c-aa43-774b-80c3-a61c98d94dc6`**（status ready）。
  - ⚠ **`add_source` 只有新增没有更新**，所以 v1.0 那份旧源（sourceId `01a0b362-8025-75d6-a4f0-cb6be2ac3a5e`）**仍在库里的同一 notebook 下、会被检索命中** → 需在知识中心界面手动删除，只留 v1.1。删之前不要引用 v1.0 的条目号。
  - 值得记一笔的反差：本次被证伪的「缩略图必须 950×500 硬校验」，**Playbook v1.0 步骤 6 里早就写对了是假规格**，错的是本笔记「发布流程踩坑 #3」。按笔记做就翻车、按 Playbook 做就对 → 附 C「先更新笔记再更新 Playbook」不是形式，两处不同步时**当次会话拿到的是错的那份**。
- **GitHub**：按用户要求**本次不推送**，只本地提交。

---

### ⑤第三轮（2026-09-25）：每次重启弹「快捷键冲突」——R 和 T 真的被原版占了

作者把两个热键改成了 R（启用/关闭）和 T（切换不避让范围），于是每次进游戏都出现快捷键重复提示；他认为 R/T 没跟任何官方或模组热键撞。**查下来不是误报，是硬冲突**，而且比弹窗更严重一层：游戏会直接把模组动作禁用掉。

**弹窗是谁发的（有行号）**：`Game/Input/InputManager.cs:1403 CheckConflicts()` → 逐个 binding 读 `ProxyBinding.hasConflicts`（`Game/Input/ProxyBinding.cs:414`），命中 `ConflictType.WithBuiltIn` 且该 binding 不是 built-in → `flag=true` → `SetModConflictNotification(map, true)`（`:1512`）→ `NotificationSystem.Push(map.name /* = AccessAnarchy */, 标题=模组显示名, text:"KeyBindingConflict", ProgressState.Warning)`。官方英文文案：标题 `Menu.NOTIFICATION_TITLE[KeyBindingConflict]` = **"Key binding conflict detected"**，描述 `Menu.NOTIFICATION_DESCRIPTION[KeyBindingConflict]` = **"Built-in key bindings"**；每次重启都重算（不是落盘的存档状态），所以每次都弹。⚠ 读 `.loc` 时发现记录里 **id 与文本相对我原来的解析错位一条**（首条记录前有额外字符串），按文本反查时要在意这个偏移。

**冲突怎么判定的**：`hasConflicts`（`ProxyBinding.cs:428`）先用 `InputManager.keyActionMap[path]` 取"所有把这个 path 用掉的 action"，`keyActionMap` 由 `ProxyAction.usedKeys`（`ProxyAction.cs:359`）填——**composite 的每一个子控件都算**（axis 的 negative/positive、vector2 的 up/down/left/right），所以"藏在组合键里"的键一样进表。然后逐条 `ConflictsWith(this, y, checkUsage: true)`（`ProxyBinding.cs:679`）：① 设备相同 ② `PathEquals`（`m_Path` + `m_Modifiers` 列表；只有一方 `disallowModifiers` 时才只比 path）③ `Usages.TestAny(x.usages, y.usages)` 非空。对侧 `item.isBuiltIn` → 置 `WithBuiltIn(1)`，否则 `WithMod(2)`。

**模组这边的 usages 是什么**：`Setting.cs:28-29` 的 `[SettingsUIKeyboardAction(name)]` 只给名字，`SettingsUIInputActionAttribute.usages` 在没有 `customUsages` 时返回 `Usages.defaultUsages`（= `BuiltInUsages.DefaultSet` = 0x41E = DefaultTool|Overlay|Tool|CancelableTool|DiscardableTool）→ `ModSetting.RegisterKeyBindings`（`Game/Modding/ModSetting.cs:188`）原样写进 CompositeInstance。**注意它不含 Menu/Options**，旧注释里"菜单+游戏内都生效"是错的，待顺手改掉。

**原版到底哪条占的（反编译改不动，直接读游戏自带输入表）**：`Cities2_Data\resources.assets` 里内置 InputActionAsset（map = Keyboard）扫到 `<Keyboard>/r` @0xacbee5c、`<Keyboard>/t` @0xacbf2d0 —— R 是 **Zoom**（`AxisSeparatedWithModifiers(m_Usages=254)`，处理器 `CameraZoom`，negative=`<Keyboard>/f`、positive=`<Keyboard>/r`）的正方向；T 是 **Rotate**（`Vector2SeparatedWithModifiers(m_Usages=254)`，处理器 `CameraRotate`）的一个方向键（同组还有 g/q/e）。usages 254 与我们的 0x41E 交集非空 → 三个条件全中 → `WithBuiltIn`。（另有一处 `<Keyboard>/r` 是 `Relocate Selected Object` 的 **modifier** 位，modifier 走 `ProxyModifier` 不进 `usedKeys`，不构成冲突，别误当成第二个撞车点。）

**比弹窗更要紧的后果**：`Game/Input/InputConflictResolution.cs:138 ResolveConflicts()` 把 built-in 当主、mod action 当次（`RefreshActions:116` 按 `isBuiltIn/isSystemAction` 分三桶，模组永远在 `m_ModActions` 即被削的一方），命中就把次方 `m_HasConflict=true` → `State.enabled=false` → `Apply()` → `ProxyAction.ApplyState(false, ...)` → **动作被 Disable**。判定用的是 `InputManager.HasConflicts(...)`（`InputManager.cs:732`），它内部 `:760` 传的是 **`checkUsage: false`**——连 usages 交集都不看，只比设备+path+modifiers。Zoom/Rotate 在 `map name = Camera`，属 `m_IsSystemAction`（`ProxyAction.cs:640` 硬编码 Camera/Tool/Editor/... 名单），城市视图里常开 ⇒ **R/T 在局内基本不会触发模组，只会缩放/旋转相机**。官方自己也是这么说的：选项页悬浮提示 `Options.CONFLICT_BUILTIN_BINDING_TOOLTIP` = "Binding conflicts with the game bindings and **will not be active**"。作者在选项页里点开关不受影响，所以"功能都正常"与"热键失效"可以并存。

**结论与处置**：不改代码——**换键位即可**，且要换成"原版压根没用过"的键。把内置表里出现的 `<Keyboard>/*` 全量枚举（脚本 `.local/input/keypaths2.mjs`，50 个 token）：字母里 **h / j / k / n 完全没被占用**；数字只有 1/2/3，功能键只有 F5/F9 → **F1–F4、F6–F8、F10–F12、4–0 都空**；`[ ] \ ; ' , - = /` 也没出现。**F3/F4 本来就在这个安全集合里**（v0.7 选它是对的）。玩家若在局内想按 R 触发，唯一"代码解法"是给动作挂一个自定义 usage（`new SettingsUIKeyboardActionAttribute(name, "AccessAnarchy")` → `Usages.TestAny` 永远为空 → 不进冲突判定），代价是热键会和相机缩放/旋转**同时**触发，且从此失去游戏对真冲突的保护 → 不建议。

**未做但记一下**：模组侧目前完全静默——`ProxyBinding.conflicts`（`ProxyBinding.cs:458`，public）能直接给出撞车对象的 map/action/path。v0.8.1 值得在 `Settings loaded` 那行后面补一条：若 `binding.hasConflicts != None` 就 `log.Warn` 打出对侧 `map/action/path`，把"为什么热键没反应"变成日志一行就能答的问题。同轮还欠一个真数据问题：**锚点网格容量饱和**（实测日志 `Anchor grid capacity hit: 18730 / 20811 anchor point(s) dropped (max 14 per cell)`，约一半锚点被丢），`FixedList128Bytes<float2>` 是硬上限、不是可调参数，要换结构才能修。

### ⑥模组名本地化（2026-09-25，v0.8.0 发布前）

作者定稿：**游戏内中文名 = 出入口无碰撞**（不是我一度听成的"通行自由"——被当场纠正，口述名字要逐字复用、不要自行意译）。英文仍 `Access Anarchy`，其余官方语言按"出入口无碰撞"的意思译，**平台显示名保持英文**。

- **平台只有一条名字**：发布接口 `research/pdxsdk/.../PublishRequestBase.cs` 只有 `displayName`（`PublishNewModVersionRequest` 只多 `changelogEntry`），游戏模组列表读 `SourceMeta.displayName` 并用 `LocalizedString.Value(...)`（`Game/Modding/ModManager.cs:298`）当原文显示，**不能按语言切换**。
- **游戏内名称的落点已经存在，不用新增 UI**：`Setting.cs` 的 12 份 locale 字典里 `{ setting.GetSettingsLocaleID(), d["mod.name"] }`，`GetSettingsLocaleID()` = `Options.SECTION[AccessAnarchy]`（`ModSetting.cs:303`）——**选项页那个模组标题就是它**，之前 12 份全填 `Access Anarchy`。所以本地化名称＝改 12 处 `mod.name` 的取值，零新控件、零新 locale key。
- 改完必须重新构建（游戏内文本没有热更新），v0.6 那次"纯英文定稿"（§0b）就是走错路线（加了括号后缀又被删），这次直接按语言给纯本地化标题，英文那条不动。

**v0.8.0 已实施的 12 语 `mod.name`（只用官方语料里确实存在的词，逐条查过 `Locale.cok`）**：en `Access Anarchy`（作者指定不变）、zh-HANS 出入口无碰撞、zh-HANT 出入口無碰撞、de *Keine Kollision an Ein- und Ausfahrten*（官方有 `Einfahrt verboten`/`Kollision`）、es *Sin colisiones en entradas y salidas*、fr *Aucune collision aux entrées et sorties*、it *Nessuna collisione a ingressi e uscite*、ja 出入口は衝突しない（官方 ja 语料里"出入口"和"出入り口"都出现过，取汉字写法；`衝突` 亦在官方文本）、ko 출입구 충돌 없음（`출입구` 见 `Assets.NAME[RooftopAccess01]=옥상 출입구`，`충돌` 31 处）、pl *Bez kolizji na wjeździe i wyjeździe*（官方 `Notifications.DESCRIPTION[No Car Access]` 就用了 `wjazdu lub wyjazdu dla samochodów`）、pt-BR *Sem colisões nas entradas e saídas*、ru *Без столкновений на въезде и выезде*（官方用 `въезда и выезда` 描述车辆进出，不用 вход/выход）。俄语/波兰语选 `въезд/выезд`、`wjazd/wyjazd` 是因为模组讲的是**车辆**出入口， pedestrian 的 вход/wejście 会跑题。

**两条通用查验手法（下次直接用）**：① **`.loc` 的方向不对称**——`en-US.loc` 是「英文原文 → locale key」的反查表，其它 11 份是「locale key → 译文」，所以拿 en 的 value 去别的语言 `Map.get()` 会得到 `undefined`；正确姿势就是在 en 里按英文文本找到 key，再用该 key 去目标语言取值（这次把两种顺序都试过才看清）。② **验证字符串有没有进 DLL 要用 UTF-16LE 搜**：`node -e "...Buffer.from(s,'utf16le')"` 扫 `Mods\AccessAnarchy\AccessAnarchy.dll`，按 UTF-8 搜会全线 MISS（连 `Access Anarchy` 都搜不到），别据此以为构建没生效。本轮 11 条新名字已全部在部署的 DLL 里各命中 1 次，`0.8.0`/`yuexian`/`不避让范围` 同时命中。

---

### ⑦键位诊断日志 + 「清空了还弹提示」的排查（2026-09-25）

作者把 R/T 清空后**仍然**每次重启看到重复提示。先按证据排除自己：`AccessAnarchy.coc` 里 `"ToggleEnabledBinding": {"m_Path": ""}` / `"m_Path": ""`，日志 `Settings loaded: ... keyEnabled=(empty), keyScope=(empty)`，而 `ProxyBinding.hasConflicts`（`ProxyBinding.cs:414`）第一句就是 `if (!isSet) return None`、`isSet => !string.IsNullOrEmpty(m_Path)`（`:600`）→ **空键位不可能进判定**，卡片标题（`InputManager.cs:1512` 用该模组显示名）必然指向别的模组。

但"盘上值 / 属性里的 ProxyBinding / 实际注册进 InputManager 的 InputBinding"是**三层**，前两层可证、第三层以前看不见，所以补了 `Setting.ReportKeyBindings(when)`：遍历 `ProxyAction.bindings`，把每条的 `device/path/modifiers`、`action.enabled`、`hasConflicts` 以及对侧 `conflicts`（`ProxyBinding.cs:458`，public，直接给 `map/action[name]=path(built-in|mod)`）打进日志。调用点两处：`AccessAnarchyMod.OnLoad`（`onLoad`）与 `AccessZoneOverlapSystem.OnUpdate` 首帧（`in-game`，此时其它模组都注册完了、游戏也在 UIReady 后算过判定，**要看就看这条**）。

顺带把"空键位到底生不生效"这条链路钉死（下次别再猜）：`ModSetting.RegisterKeyBindings` 先按 `[SettingsUIKeyboardBinding]` 的默认键建动作 → `LoadSettings` 的 `[AfterDecode] ApplyKeyBindings`（`ModSetting.cs:270-277`）调 `InputManager.SetBindings` → `SetBindingImpl` 只在 `!compositeInstance.canBeEmpty` 时拒绝空（`:997`，而 `canBeEmpty` 缺省 **true**，`RegisterKeyBindings` 并不从属性复制它）→ `TrySetMainBinding` 写 `binding.overridePath = ""` → **Unity 的 `effectivePath => overridePath ?? path`**（`Unity.InputSystem.dll` 反编译 `InputBinding.cs:246`，是 `??` 不是判空串）→ 空串确实清掉了绑定。

**同机扫描出来的真凶候选**（脚本 `.local/input/scan-mod-bindings.mjs`，64 个 `.coc`）：这些模组把热键压在**原版输入表里存在的裸键**上，每一个都会生成自己名字的卡片——BetterTransitView `x`、K45_EUIS `z`、K45_WE `pageDown/pageUp/space/home/四方向键`、TimeWeatherAnarchy `b`、Mods_Yenyang_Anarchy `pageUp/pageDown`、MoveIt `m + pageUp/pageDown`、RoadBuilder `l`；带修饰键的（Ctrl+Z、Ctrl+Shift+W 等）不算，`pathAndModifiersComparer` 会比修饰键。**还有一条要给作者**：他自己的 `Settings.coc` 已把**原版**改成 F1=Switch Toolmode、F2=Toggle Snapping、**F3=Toggle Contour Lines**、Tab=Transportation Panel，所以"换回 F3/F4"这个直觉选项里 **F3 现在是占用键**，安全裸键是 h/j/k/n、F6-F8、F10-F12、数字 4-0（全量表见 `.local/input/bindings.txt` 与 §⑤ 的 keypaths 结果）。

---

## 发布记录（v0.8.0，2026-09-25）

- **Paradox Mods**：ModId 158842 平台版本 3 → **4**，`userModVersion` **0.8.0**。命令 `dotnet publish -p:PublishProfile=PublishNewVersion -c Release`，工具链回显三连：`Publishing new mod version` → `New mod version published` → `Publishing new version updating process finished successfully`，且它打印的元数据里 changelog 正文（英文 6 条 + 「中文」6 条）逐行完整、`- ` 项目符号保留。
- **⚠ 线上回读这次没做**：想循环打 `api.paradox-interactive.com/mods?modId=158842&os=windows` 确认新副本时，被当前会话的权限策略拦下（判定为"未明确授权的外部请求"）。所以**"平台上的 0.8.0 已经能被玩家读到"这一点本轮未实测**，只有工具链的成功回显为证。下次要补验证就单次 `curl` 一两条（别循环十几次），或让用户自己看模组页 ALL VERSIONS。
- **元数据只动了两处**：`<ModVersion Value="0.8.0">` 与 `<ChangeLog>`（一条 = 一个平台版本，绝不夹带历史版本）。`<DisplayName>` 仍 **Access Anarchy**（发布接口只有一个 `displayName`，平台名不能按语言切换，作者 2026-09-25 明确"平台名保持英语"），描述/封面/截图/标签/外链都没动——注意 `FillPublishMetadata` 每次都会**全量重推**这些，所以 `<Thumbnail>` 必须继续指着 `Properties/Thumbnail.jpg`（作者海报），指回 `research/ThumbGen` 产物就会覆盖线上封面（v0.7.8 的老坑）。
- **发布后按作者要求删掉了本机构建部署目录** `...\Cities Skylines II\Mods\AccessAnarchy\`（只有 6 个产物文件，没有配置）。**设置档 `...\Cities Skylines II\AccessAnarchy.coc` 保留**（键位仍是清空状态）。下一次进游戏加载的就是 Paradox 订阅缓存里的 `...\.cache\Mods\pdx_mods\158842_4\AccessAnarchy.dll`——**日志第 2 行 `Current mod asset at ...` 要看到 `pdx_mods\158842_4` 才算验的是线上版**（2026-09-24 那次差点白测的教训）。以后 `dotnet build -c Release` 会重新生成 `Mods\AccessAnarchy\`，届时本地副本又会和订阅副本并存，谁赢仍以日志第 2 行为准。
- **线上版带着键位诊断**：`Key binding check onLoad` / `Key binding check in-game` 两行（见 §⑦）就在这一版里，所以删掉本机构建后照样能查"重复提示是谁发的"。
- **GitHub**：已提交并推送（`a07c584` 功能提交 + `c319df1` 合并提交，远端 `main` 已指到 `c319df1`）。**又遇到分叉**：远端 5 条里 4 条是本机同名提交的 Contents-API 副本（当年用 API 直推 trees 会在远端另生成 SHA），只有 `75ed436 Add Ko-fi username`（`.github/FUNDING.yml`，ko_fi: yuexian7）是真新增，其余文件本机版本更新（v0.7.9 换 `Properties/Thumbnail.jpg` 并删占位 png、v0.8.0 全部改动）。处理方式沿用 `8c346a9` 的先例：**`git merge --no-commit -s ours origin/main` 把远端收成祖先、树保持本机，再单独 `git checkout origin/main -- .github/FUNDING.yml` 取回那个文件**，然后 push（此时是快进）。不要 `pull --rebase`：那 4 条副本内容相同但 SHA 不同，重放会在 `PublishConfiguration.xml` / 开发笔记 上撞同一区域；也不要 `merge -X theirs`，那会把远端的旧封面/旧 changelog 合并回来。

---

## 发布记录（v0.7.8，2026-09-19）

- **Paradox Mods**：ModId **158842** 推出新内容版本，`userModVersion` 0.6.0 → **0.7.8**，os=windows，requiredVersion 仍 `1.6.*`，AccessLevel 仍 **Public**。命令：`dotnet publish -p:PublishProfile=PublishNewVersion -c Release`，返回 `Publishing new mod version` → `New mod version published` → `Publishing new version updating process finished successfully`。上传的是当时的 Release 构建（0 错误 0 警告，Burst 三平台 7 methods）。
- **本轮元数据改动**（用户口述原文照搬，中英同步）：① longDescription 警告段整体替换——beta 阶段 + v0.7 性能大幅优化（57×57 km、人口 10 万实测帧数几乎不变）+ 数十个主流 MOD 无冲突/无报错 + 理论上不改车辆行人路径、只与改碰撞逻辑的 MOD 冲突 + 有问题去讨论区反馈；② changelog 写 v0.7.8 一条（性能大幅提升 + 快捷键 F3/F4，游戏中用快捷键开启可能短促卡顿属正常）。**首发时 ② 用错了结构（多版本子节点），发布后已按下面的机制正本改成 BLG 同款单版本格式**。
- **线上实测（2026-09-19 发布后回读，`node research/live-changelog.mjs 158842`）**：平台版本 2 / 自报版本 0.7.8 / 148,271 B / released 2026-09-19T14:36Z；`changelog` **只有 1 条**（挂在平台版本 2 上），正文是 v0.6.0 + v0.7.8 两段被压成的**一整段**（换行全丢）；封面 `cover_2.jpg` + 2 张截图仍在；订阅 586→589。网页端 `LAST UPDATED 2026-09-19 22:36` 与条目时间 `2026-09-19 14:36` 是**同一时刻**（页面 Last Updated 走本地时区、changelog 条目走 UTC），不是 bug。
- **ChangeLog 机制（本次踩错，正本记这里，下次照抄）**：
  1. 线上 changelog 是**「一个平台版本一条条目」**，条目日期由该版本的发布时间自动生成、作者不可控。所以 `<ChangeLog>` 里**只允许写当前这一版**的内容，绝不能把历史版本的日志一起塞进去——那样只会被拼成一条挂在最新版本名下，等于给旧版本造假日期。v0.6.0 的日志线上**从来就没有条目**（首发 `PublishRequest` 没有 `changelogEntry` 字段，见「changelog 机制」条），也不是这次删掉的。
  2. **不要用子节点分版本**（`<ChangeLog><v0.6.0>…</v0.6.0><v0.7.8>…</v0.7.8></ChangeLog>`）：工具取的是拼接正文，**换行在拼接中丢失**，平台按 Markdown 渲染成一整段（实测就是这样糊成一坨）。正确写法照 BLG `releases/v1.0.3/PublishConfiguration.xml` L189-215：`<ChangeLog>` 用**元素文本形式**、**行首不缩进**、首行版本字面量 `v0.7.8`、空行、`- ` 英文条目、空行、`中文`、空行、`- ` 中文条目。有 `- ` + 空行才会分段。
  3. **已上传的条目改不掉**：平台不改写历史条目（BLG 同样结论）；`Update`/`Patch` 走 `ModsPublishingService.cs` L217 `PatchModVersion(FillPublishMetadata(...))`，而 `FillPublishMetadata`（L524-542）**根本不设置 `changelogEntry`**（只有 L208-213 的 NewVersion 分支设置）→ 想把干净的正文推上线只能再发一个内容版本（要占一个新自报版本号）。本次 XML 已改成规范格式，**线上下次真发版时自然替换**（用户已拍板：不为修文案单独烧版本号，见下方「决定」）。
  4. 发布输出里那条 `EXEC : warning : this mod is currently still in **beta**...` 是 MSBuild 把描述中以 `Warning:` 开头的行当成告警前缀，**纯显示副作用**，内容照样按原文上传。
- **线上真值免 token 查法（本次新确认，比 database.json 那套简单）**：`https://api.paradox-interactive.com/mods?modId=158842&os=windows` **是公开只读接口，不需要任何会话 token**（正本脚本 `research/live-changelog.mjs`，另一份带版本清单/截图/描述长度的 `D:\WorkSpace\CS2MOD\BridgeTheLanguageGap\scripts\live-status.mjs 158842` 可直接跑）。§「平台 API 直查方法」里那套 token 读法是多余的，除非要写操作。
- **v0.7.0–v0.7.7 从未上线**：线上是 v0.6.0 → v0.7.8 直接跳。撞击反馈（BloodEffects/Stumbling）v0.7.3 就已删除、且从未公开发布，所以本次 changelog 不提它是正确的，不要事后补。
- **GitHub**：`yuexian7/AccessAnarchy` main 已同步本次改动（源码 + README + 本笔记 + `PublishConfiguration.xml`）。
  - 远端 tip 每次由脚本末行 `PUSH_OK main -> <sha>` 给出（最后一次 `58a1e78`，其 tree 与本地 HEAD 的 tree **逐字节相同**）；github.com 传输恢复后 `git fetch && git rebase origin/main` 让本地 commit sha 对齐，再回到正常推送。
  - **github.com:443 会被墙、api.github.com 始终通**：git push/fetch 失败时改用 `GH_TOKEN=$(powershell.exe -NoProfile -ExecutionPolicy Bypass -File research/get-token.ps1 | tr -d '\r\n') node research/push-gitdata.mjs`（token 取自 Windows 凭据管理器里的 GCM PAT）。
  - **该脚本第一版把文件推成一坨 base64**：`POST /git/trees` 的 `content` + `encoding:'base64'` **不解码**，原样存成文件内容（远端 `PublishConfiguration.xml` 一度变成 `PD94bWwg...`）。已改成「先 `POST /git/blobs`（这个会解码）拿 sha、再用 sha 建树」，并在每次上传后**与 `git hash-object --no-filters` 比对 sha**、推完用 Contents API `Accept: application/vnd.github.raw` 回读逐字节核对。旧脚本 `research/push-via-api.ps1` 用的是同一个坑写法，别再照抄。
- **决定（2026-09-19，用户拍板）**：**不为修文案单独发版**——线上那条混了 v0.6.0 文字的 0.7.8 条目就这么留着，等下次真发版（v0.8.x）时新条目自然变成干净的单版本日志。`<ChangeLog>` 已经是规范格式，下次发版直接改正文即可。
- **待办**：下次发布 checklist 照抄下一节，只需把「实机验证」视为已过；发完跑 `node research/live-changelog.mjs` 确认新增的是一条、正文只有当前版本。

---

## 发布记录（v0.6.0，2026-09-12）

- **Paradox Mods**：ModId **158842**，`Access Anarchy` v0.6.0，~~Unlisted~~ **Public**（2026-09-12 已改公开），os=windows，requiredVersion `1.6.*`，tags `[Code Mod]`，GitHub + Ko-fi 外链、论坛帖 ForumLink 均已配。线上地址 `https://mods.paradoxplaza.com/mods/158842/windows`。
- **GitHub**：`https://github.com/yuexian7/AccessAnarchy`（public），12 文件。
- **平台同步状态（2026-09-13 核对，平台→本地已回填）**：线上用户在网页端改过 4 处，已同步进 `Properties/PublishConfiguration.xml`：① longDescription 换成了中英双语版（同步时把换行写成 `&#xA;` 实体——**XML 属性值里的字面换行会被解析器规范化成空格**，必须用实体）；② AccessLevel Unlisted→Public；③ ExternalLink 增加 kofi（`https://ko-fi.com/yuexian7`）；④ ForumLink 回填（`https://forum.paradoxplaza.com/forum/threads/access-anarchy.1941285/`，模板节点名是 `ForumLink`，单值非数组）。线上快照存 `research/platform_modDetail_20260913.json`（GetDetails API 原始返回），截图/封面备份在 `research/platform_media/`（线上截图 2 张：1713×1057、1805×1212，本地 Properties 没有对应源文件，只有封面缩略图 950×500 PNG）。
- **2026-09-14 全字段复核（平台↔本地逐项 diff，脚本比对 GetDetails JSON vs XML）**：displayName / shortDescription / longDescription（含 `&#xA;` 实体还原后逐字符相等）/ tags=[Code Mod] / acl=public / requiredVersion=1.6.* / GitHub+Ko-fi 外链 / userModVersion=0.6.0 全部一致，**XML 零改动即与线上同步**。平台 `latestUpdate=2026-09-12T11:05Z`、latestVersion=1（09-13 的网页端改动属元数据编辑未推新 content 版本）。统计增量：订阅 145→**245**、评分数 4→**13**（均分 5.0）。当日 GetDetails 原始快照存 `research/platform_modDetail_20260914.json`。**线上不存在 0.6.1 或任何 v0.7 版本**——发布前一切以本段和 GetDetails 实查为准。
- **平台 API 直查方法（下次照抄）**：会话 token 在 `C:\Users\10975\AppData\LocalLow\Colossal Order\Cities Skylines II\.pdxsdk\76561198305266033\database.json` 的 `SessionToken` 字段（36 位 GUID，24h 过期需开游戏刷新）。GET `https://api.paradox-interactive.com/mods?os=windows&arch=x64&modId=158842`，Authorization header 填 `{"session":"<token>","type":"Session"}`（JSON 字符串本体，不是 Bearer）。版本列表：GET `.../mods/versions?modId=158842`。
- **changelog 机制（重要）**：首发 PublishRequest **没有 changelogEntry 字段**（`ModsPublishingService.cs` 里只有 NewVersion/Patch 请求带），所以线上 `changelog:[]` 是首发机制限制、不是发布遗漏。XML 里的 `<ChangeLog>` 节点会在下次 `PublishNewVersion` 时自动作为 changelogEntry 上传。同理 NewVersion/Update 每次都会重推全部元数据（`FillPublishMetadata`：displayName/short/longDescription/tags/thumbnail/screenshots/acl/externalLinks 等），**网页端手动改的描述若没先回填 XML，下次发布会覆盖丢失**。
- **发布流程踩坑（本机特有，下次直接照抄）**：
  1. Windows **没装 git/gh**，WSL 有 git 但 github.com 被墙（api.github.com 通）。解法：git 仓库在 WSL init/commit；推送走 **GitHub REST Contents API**（PowerShell + Windows 凭据管理器里 GCM 存的 PAT——blob 是 **UTF-16** 编码，先按 Unicode 解码再剥控制字符）。空仓库直接用 Git Data API 建树会 409「Git Repository is empty」，**用 Contents API 逐文件 PUT**（自动建初始 commit）。
  2. PowerShell 5.1 把无 BOM 的 .ps1 按 **GBK** 解码——中文注释会让 here-string 解析炸掉（BLG §10 #28 的坑在本机复现），`.ps1` 必须纯 ASCII；含中文的数据一律由 node 生成 JSON、PowerShell 只发 HTTP。
  3. ModPublisher 首发校验：`<GameVersion Value="1.6.*" />`（缺了报 `GameVersion must be set`）、`Thumbnail` **只要求文件存在**（~~950×500 8-bit RGBA PNG 硬校验~~ 这条是当年误记，2026-09-20 反编译 `PrepareThumbnail` 证伪，见「发布记录（v0.7.9）」；`BaseOptions.cs` L405 只判 `IsNullOrEmpty`）；**Update 命令还要求 `<ChangeLog>`**（首发 Publish 不要求）。
  4. **`<Thumbnail>` 是模组页「标题旁那张方形封面」**（线上 `displayImagePath` / `cover_N.jpg`），不是下面的 `<Screenshot>`。现在直接放作者做的海报原图 `Properties/Thumbnail.jpg`（1254×1254，不裁切不加边框）。`research/ThumbGen/` 是**早期自造的 950×500 占位图生成器，已废弃**——它一旦被重新生成回 `Properties/Thumbnail.png`，下次发版就会把线上封面覆盖成占位图（v0.7.8 就是这么翻车的）。要把源图转成指定规格用 `research/ThumbFrom/`（`dotnet run -c Release -- <src> <out> [native|crop|blurpad|solidpad|stretch]`，`native` = 原尺寸重编码）。
  5. pubxml 三件套在 `Properties/PublishProfiles/`（Publish=首发、NewVersion=重传二进制、Update=只改元数据）。**重传二进制用 NewVersion，别用 Update**（Update 会把 ModVersion 一起推上去，烧版本号——BLG §8 硬规则）。
- **下次发布 checklist**：改版本（Mod.cs 横幅 + xml ModVersion + ChangeLog）→ `dotnet build -c Release` → 实机验证 → **先把平台端手动改动回填进 xml（见上平台同步状态）** → **确认 `<Thumbnail>` 仍是 `Properties/Thumbnail.jpg`（作者海报），没被改回 ThumbGen 占位图，否则线上封面又会被打回去** → `dotnet publish -p:PublishProfile=PublishNewVersion -c Release` → 查线上 API 确认 `size_in_memory` 变化 + `changelog` 新增一条 + `displayImagePath` 是海报（**Varnish 双副本要循环打十几次**）→ 同步 xml 到 GitHub。

---

## 0. v0.7.0 修订（性能重构 + 快捷键，2026-09-14，先读这节）

### 0.6 v0.7.2 性能架构重建（2026-09-15，用户：「帧数根本没变」）

**结论先说**：v0.7.0/v0.7.1 的优化方向（集合过滤、O(邻域)）**不是主瓶颈**。
真正的成本是【每帧和游戏抢写 buffer】的写战 + 集合重建时的主线程 Complete/ToEntityArray。

#### 根因（机制层）

| 现象 | 机制 |
|---|---|
| C 每帧 `ECB.SetBuffer<LaneObject>` 删邻域行人注册 | `HumanNavigationSystem` 随后把行人注册回去 → **每帧 structural write-war**（ECB、脏 buffer、重加） |
| A 每 4 帧重刷 overlap | `LaneOverlapSystem` **只在世界加载与「拥有者实体」（带 SubLane 缓冲的 edge/node/building）带 `Updated` 时**重建 overlap（不是车道 `Updated`，见 §0.7⑤ 的更正）。平时我们删掉的条目会一直躺着，每 4 帧重刷纯浪费，还制造写放大 |
| 集合重建 | `Dependency.Complete()` + 全城 `ToEntityArray`（行人道+全部车辆 overlap 车道）→ 大地图周期性卡主线程；重建日志也是 I/O |
| 血迹检测 | 开着时对邻域每个行人每帧四叉树查询（v0.7.1 已降到 4 帧，仍贵） |

**为什么「删 overlap」就够、不需要「删 LaneObject」**：车辆看见行人只经
`CheckOverlappingLanes` → 对端行人道 `LaneObject` → `CheckPedestrian`。
A 把车辆车道侧指向行人道的 overlap 删掉后，车辆逻辑上再也读不到那些行人道；
C 再每帧删注册是冗余备份，却是写战主因。
行人真走上车行道本体（`CheckCurrentLane`）极罕见，维持原版行为（§3 已知限制）。

**试过但不可用的捷径**：给行人道加 `SecondaryLane` 可让 `LaneOverlapSystem` 永久跳过
（`LaneOverlapSystem.cs` L1043/L1116），但 `LaneSystem` L1978 会把带 `SecondaryLane` 的
车道从行人/车/自行车路径方法里剔除 → **会弄坏行人寻路**，弃用。

#### v0.7.2 新架构（低频一次性数据编辑）

```
每帧常态（access 模式、blood 关）：
  空转 ≈ 0（只读 Setting + 偶发心跳日志）

每 2048 帧（或开关/模式变化）：
  Complete → 重建锚点网格 → 收集全城候选 → 分帧 2048/帧筛进邻域集合
  扫完交换双缓冲 → 立刻对活跃车辆集合跑一次 A（StripAccessOverlapsFromSetJob）

每 120 帧安全重刷 A（覆盖修路后 LaneOverlapSystem 的 Updated 重建）

blood 开：仅检测，每 8 帧扫邻域行人道四叉树；m_DoStrip=false，不再写 LaneObject
```

| 项 | v0.7.1 | v0.7.2 |
|---|---|---|
| C 删 LaneObject | access 模式每帧 | **删除该职责** |
| A 频率 | 每 4 帧 | 集合就绪时 1 次 + 每 120 帧 → **v0.7.4 改为 Updated 事件驱动** |
| 集合构建 | 每帧持续增量 | 每 2048 帧一轮，分帧 2048 |
| 重建日志 | 每轮一行 | 去掉 |
| 心跳日志 | CalculateEntityCount×3 | 只打集合 Count |
| 血迹 | 检测+删注册 | 仅检测，8 帧 → **v0.7.3 整个功能删除** |

**全局模式**：世界加载后做一次全量 `ScheduleParallel`，之后只走 `Updated` 事件驱动（v0.7.4）。

#### 待实机验证

1. access 模式：帧数应接近关模组（对照：选项里关掉 Enabled 再比）。
2. 出入口穿透保持；横道避让保持（v0.7.7 起仅出入口模式保持，全局模式横道也穿透）；修路后**当帧**自动恢复穿透（v0.7.4 事件驱动）。
3. 心跳日志：`vehicle set N`，N 应为几百～几千，不是全城数万。

### 0.7 v0.7.3–v0.7.4 修订（删除撞击反馈 + 事件驱动重刷，2026-09-16）

#### ① 删除撞击反馈（BloodEffects）

**用户实机反馈（v0.7.1）**：行人碰撞功能根本没效果——踉跄动画从未触发。

**决定**：直接删除该功能及设置项。理由：
- 检测逻辑依赖邻域行人道 LaneObject buffer 中的行人注册 + 移动对象四叉树查询，
  但 v0.7.2 已去掉 C 的删除职责（m_DoStrip=false），检测路径本身理论上应工作；
- 未验证的嫌疑：Stumbling 组件可能需要额外前置状态（如行人正在寻路、有 path）才能驱动
  HumanNavigationSystem.UpdateStumbling 分支；或四叉树查询时机与车辆位置不匹配；
- 用户已明确「做不了就去掉」，不再投入调试。

**删除范围**：
- Setting.cs：`BloodEffects` 属性、`m_BloodEffects` 字段、SetDefaults 条目、12 语言 `blood.label`/`blood.desc` 词条、Build() 中的 locale 注册
- AccessZoneOverlapSystem.cs：`StripPedestrianOnNearbyWalkwaysJob`、`BloodHitIterator`、`RemoveStumblingJob` 三个 struct；`m_StumblingUntil`、`m_ObjectSearchSystem`、`m_PedSets`、`m_PedestrianQuery` 四个字段；`kBloodInterval` 常量；OnUpdate 中的 blood 调度块；`Game.Creatures`/`Game.Objects`/`Game.Vehicles` 三个 import
- 连带简化：`needAccessSets` 从 `!globalMode || bloodEffects` 简化为 `!globalMode`；LogStatus 去掉 ped set 和 blood 输出；AdvanceAccessSets 去掉 ped set 构建和行人道候选收集；BuildAccessSetsJob 去掉 `m_PedestrianLaneData`/`m_BuildPedSet` 字段和行人道分支

#### ② v0.7.2 帧数分析（代码审查结论，未实机）

**结论：v0.7.2 的修改方向正确，应对帧数有显著改善。** 三个改动各自砍掉一块成本：

| 改动 | 砍掉的成本 | 量级 |
|---|---|---|
| 去掉 C 的每帧 `SetBuffer<LaneObject>` | 每帧对邻域行人道的结构性写 → HumanNav 重加 → 脏 chunk → ECB 回放，写战循环彻底消失 | **最大头**（ECS structural change 是最贵操作之一） |
| A 从每 4 帧 → 每 120 帧 | 30× 频率削减；access 模式只遍历邻域集合（几百条） | 大 |
| 集合重建从每帧增量 → 每 2048 帧整轮 | 去掉每帧 IsNearAnchor 候选推进 + 周期性 Complete/ToEntityArray 主线程卡顿 | 中 |

**残留风险**：120 帧安全重刷是防御性定时器——平时 overlap 不被游戏重写（LaneOverlapSystem 只在世界加载和车道 `Updated` 时重建），绝大多数时候是空转，但仍是周期性 spike。

#### ③ 方案 B 已实施（v0.7.4，2026-09-16）：事件驱动重刷

反编译证据（`LaneOverlapSystem.cs` L1893-1919 + `SystemOrder.cs` L178/L310 + `CleanUpSystem.cs` L53）：
- LaneOverlapSystem 在 **Modification4B（阶段 13）** 运行，早于 CarNavigationSystem（GameSimulation，阶段 18）
- `Updated` 是单帧脏标记，**CleanUpSystem（Cleanup 阶段，最后一帧）才移除**
- 所以同帧内：工具加 Updated → LaneOverlapSystem 重建 overlap → **本系统仍能看到 Updated** → 当帧重删 → CarNav 读到干净数据

**实现**：
- 新增 `m_RefreshQuery`：`All=[LaneOverlap, Updated], Any=[CarLane, ParkingLane, GarageLane, ConnectionLane, AreaLane], None=[Deleted, Temp]`
- 每帧对 `m_RefreshQuery` 调度 `StripPedestrianOverlapsJob`（稳态匹配 0 实体，成本 ≈ 0）
- access 模式三重判定：`IsAccessLaneByType || m_AccessVehicleSet.Contains || IsNearAnchor`（捕捉新建道路）
- 全局模式世界加载后首次全量 A（`m_InitialGlobalApplyDone` 标记），之后只走事件驱动
- 删除 `kReapplyInterval`（120 帧定时器）、`m_SetsReadyFrame`、`m_UseSet`（死字段）
- `StripPedestrianOverlapsJob` 新增 `m_CurveData` + `m_Grid` 字段 + `IsNearAnchorLocal` 辅助方法

| | v0.7.2（方案 A） | v0.7.4（方案 B，已实施） |
|---|---|---|
| 稳态每帧成本 | 0（空转） | 0（query 匹配 0 实体） |
| 周期性 spike | 每 120 帧扫整个邻域集合 | **无** |
| 修路后恢复延迟 | 最多 120 帧（~4 秒） | **当帧** |
| 新建道路邻近锚点 | 等集合重建（最多 2048 帧） | **当帧**（IsNearAnchor 判定） |

构建 0 错误 0 警告，Burst 三平台 4 methods，已部署本地 Mods 目录。**未发布**。

#### ④ 开关失效修复（v0.7.5，2026-09-16）：关闭时恢复 overlap

**用户实机反馈**：「一旦启用后关闭还是会穿过行人」——开关失效，关闭后车辆仍不避让。

**根因**：`OnUpdate` 在 `!Enabled` 时直接 `return`，但我们此前用 `ECB.SetBuffer<LaneOverlap>`
删掉的行人 overlap 条目**不会自动还原**。原版 `LaneOverlapSystem` 只在车道带 `Updated` 脏标记
（修路/建拆）时才从头重建 overlap；单纯关闭本模组不产生任何 `Updated`，删除因此永久残留。
这与 §0.7③ 的事件驱动重刷是同一枚硬币的两面：`Updated` 既能触发「重建后我们再删」，
也能被我们主动打上「触发重建以恢复」。

**修复**（`AccessZoneOverlapSystem.cs`）：
- 新增 `m_WasEnabled` 字段，捕捉 启用↔关闭 跳变。
- 启用→关闭跳变时调 `ScheduleRestoreOverlaps()`：对 `m_OverlapQuery`（全城车辆车道）
  调度新 job `MarkLanesUpdatedJob`（IJobChunk），`ECB.AddComponent<Updated>`。
  下一帧 LaneOverlapSystem（阶段 13，早于本系统 GameSimulation 阶段 18）看到 Updated →
  重建 overlap → 行人条目恢复；CleanUpSystem 帧末移除 Updated。此时本系统已 return，不会重删。
- 同时清空增量状态（`m_InitialGlobalApplyDone`/`m_SetsInitialized`/`m_BuildInProgress`/`m_Cursor`/
  `m_LastMode`/`m_LastIncludeMask`），下次启用走全新首次全量 A + 重建邻域集合。
- 成本：关闭瞬间一次全城 overlap 重建（≈ 世界加载量级），仅跳变时发生一次，可接受。

构建 0 错误 0 警告，Burst 三平台 **5 methods**（新增 `MarkLanesUpdatedJob`），已部署本地 Mods 目录。**未发布**。
待实机验证：启用→关闭后车辆恢复避让行人；关闭→再启用穿透恢复；横道避让始终保留。

**实机结果（2026-09-18，用户）：完全没生效**——关闭后车辆仍然永远不避让，切换作用模式也无效。
上面那段根因里「LaneOverlapSystem 只在**车道**带 Updated 时重建」这句话是错的，见 §0.7⑤。

#### ⑤ 开关失效修复 v2（v0.7.6，2026-09-18）：重建按"拥有者实体"算，不按车道

**用户实机反馈（v0.7.5）**：「重进存档后确实会避让行人，但一旦开启不避让就没法关闭了，
车辆永远也没法避让，无论切换全部道路还是怎么样都不生效。」
—— 前半句（重进存档能恢复）本身就是最关键的线索：读档能恢复，说明恢复机制只有读档那一条路。

**真正根因（逐行复核 `research/decompiled/Game/Net/LaneOverlapSystem.cs`）**：
原版重建的**闸门**和**工作清单**都以「拥有者实体」为单位，车道实体的 `Updated` 完全不参与重建：

| 位置 | 事实 |
| --- | --- |
| L1893 | `m_UpdatedOwnersQuery = [SubLane, Updated, !Deleted]` —— `SubLane` 是**动态缓冲**，只挂在 edge/node/building 这些"车道拥有者"上，车道自己只有 `Lane` |
| L1920 | `if (!entityQuery.IsEmptyIgnoreFilter)` —— 闸门只看上面那个拥有者查询。v0.7.5 只给车道打 Updated → 闸门永远不开 |
| L1926 | `nativeList = entityQuery.ToEntityListAsync()` —— 工作清单同样是拥有者 |
| L1030-1035 | `UpdateLaneOverlapsJob.Execute(i)` → `UpdateOverlaps(entity, m_SubLanes[entity], ...)`，L1077 `bufferData.Clear()` 后逐条重算：重建单位是拥有者的 SubLane 列表 |
| L1919/L2009 | 车道的 `Updated` 只喂 `m_UpdatedLanesQuery` → `CollectLaneDirectionsJob` / `UpdateLaneFlagsJob`（刷 CarLane Yield/Stop/Approach 等派生标记），不负责重建 |
| L1904-1912 | `GetLoaded()`：读档后第一轮走 `m_AllOwnersQuery` + `m_AllLanesQuery` + `m_UpdateAll=true` 的全量重建 —— **这就是"重进存档就恢复"的那条路** |

v0.7.5 打上的 `Updated` 因此在 CleanUpSystem 帧末被清掉，什么都没重建，删除永久残留。

**v0.7.6 修法（三层）**：
1. **主路径** `TryForceVanillaOverlapRebuild()`：反射取 `LaneOverlapSystem` 的私有 `m_Loaded` 字段
   （L1884，除 `GetLoaded()` 外无其他读者）并置 true → 下一帧该系统走**与读档完全相同**的全量重建分支。
   选它作为主路径的理由：这条路径的有效性已被用户实机证明（重进存档就恢复），且不依赖 `Updated` 语义。
   `World.GetExistingSystemManaged<LaneOverlapSystem>()` 拿到的就是真实例——CS2 只有一个模拟世界
   （GameManager `new World("Game")` == `World.DefaultGameObjectInjectionWorld`），
   该系统在 `SystemUpdatePhase.Modification4B`（SystemOrder.cs:178），早于本系统的 GameSimulation。
2. **兜底路径** `ScheduleMarkUpdatedRestore()`（反射失败/审计不通过时才跑）：
   `m_OwnerQuery`（`SubLane` + !Deleted/Temp，并排除 `Lane`/`LaneOverlap` 以与车道侧互斥）
   + `m_RestoreLaneQuery`（`Lane` + !Deleted/Temp/SecondaryLane，== 原版 `m_AllLanesQuery` 口径）
   两类实体各自打 `Updated`（job 里先 `ComponentLookup<Updated>.HasComponent` 跳过已有的，
   避免同一 ECB 对同一实体重复 AddComponent）。拥有者开闸进清单，节点还会被
   `AddNonUpdatedEdgesJob`（L41-51）带出相邻 edge；车道侧补齐 flags 两趟 → 与 `m_UpdateAll=true` 等价。
   **注意**：兜底口径必须是「全部车道」而不是「车辆车道」，否则行人道侧的派生标记不刷新（§复核结论）。
3. **确定性自检** `TickRestoreAudit()`：跳变后 `kRestoreAuditDelayFrames`(=6) 帧跑一次
   `AuditPedestrianOverlapsJob`（分块并行，每块两个 int 计数，主线程求和，同步取结果），
   统计车辆车道上残存的行人 overlap 条目数：
   - `> 0` → 日志 `Restore audit: pedestrian overlaps back on N vehicle lanes` = 恢复成功；
   - `== 0` 且未升级 → 自动补跑兜底路径并再审计一轮（`m_RestoreEscalated` 保证最多两轮）；
   - 两轮后仍 `== 0` → 日志直接给出「LaneOverlapSystem 没有重建」的结论，不再靠猜。

**顺带修的同类残留**：`范围收窄`（global→access、或任一 Include* 取消勾选）以前只重置增量状态，
先前在全局/大范围下删掉的条目留在范围外车道上 → 表现为"怎么切都没反应"。
现在 `narrowed` 分支走 `TriggerVanillaOverlapRebuild("scope narrowed")` + `m_ReapplyCountdown`
等原版重建落地后再按新范围重删（范围变宽不需要还原，多删即可）。
`m_ScopeObserved` 保证启用后第一次读到设置时不做无谓还原（读档数据本来就干净）。

构建 0 错误 0 警告，Burst 三平台 **7 methods**（新增 `MarkOwnersUpdatedJob`、`AuditPedestrianOverlapsJob`），
已部署本地 Mods 目录。**未发布**。
待实机验证：① 启用→关闭后车辆恢复避让（看 `Restore audit` 行）；② 关闭→再启用穿透仍生效；
③ 全局模式下切回出入口模式后，范围外道路恢复避让；④ 横道避让始终保留；
⑤ 关闭瞬间与收窄瞬间各有一次卡顿（≈ 读档量级，只一次）。

**实机结果（2026-09-19，用户）**：①②③ 通过（「现在会避让了」），但 **④ 反过来不成立**——
「全部道路」模式下车辆在人行横道永远避让行人，用户判为"切换到全部道路时不生效"。→ v0.7.7，见 §0.7⑥。

#### ⑥ v0.7.7：全局模式补横道 + 设置项改名「避让范围」（2026-09-19）

**根因（不是 bug 级机制误判，是自家口径自相矛盾）**：干预点 A 里对
`PedestrianLaneFlags.Crosswalk` 的排除是**无条件**的（v0.5 为修"出入口半径误伤横道"加的），
于是全局模式实际是"全部道路**除了**人行横道"，而 12 种语言的 `mode.desc` 早就写着
「全部道路（全局）：车辆在任何地方都不再让行行人，**包括人行横道**」、
`mode.warning` 写「全局模式会禁用全市所有车辆对行人的避让」、README 同一条款——
文案与实现不符，用户按文案理解，故判定"不生效"。

**机制复核（判定"删横道条目就够了"）**：`research/Game.Simulation.CarLaneSpeedIterator.cs`
1174 取本车道 `LaneOverlap` → 1193 遍历条目 → 1346 对端 `LaneObject` 里带 `Creature` 的
→ 1348/1403 `CheckPedestrian` 压低 `CarNavigation.m_MaxSpeed`。全树 grep `Crosswalk` 只出现在
CompositionSelectSystem / GeometrySystem / LaneSystem / PedestrianLaneFlags / SecondaryObjectSystem /
PathUtils / InitializeObsoleteSystem / NetToolSystem，**没有任何"横道专用"的车辆让行通道**，
所以删掉 car→crosswalk 的 overlap 条目即可让车不再为横道减速。
（有信号灯的路口仍会因红灯停车，那是 `LaneSignal`，不是避让行人，维持原版。）

**改法**：`IsPedestrianLaneEntity(..., int includeCrosswalks)` 新增判据参数；
`StripPedestrianOverlapsJob.m_IncludeCrosswalks` = 全局首次全量删 1、每帧事件驱动重刷 `globalMode ? 1 : 0`；
`StripAccessOverlapsFromSetJob` 恒 0（只在出入口模式跑，v0.5 的误伤修复语义不变）。
`global→access` 属"范围收窄"，已由 ⑤ 的还原+重删路径负责把横道条目要回来（无需新增逻辑）。
审计 `AuditPedestrianOverlapsJob` 计数改为每块 4 个 int（行人条目总数 + 其中横道条目数），
理由：全局模式下横道条目也被删，只数非横道会在"其实已恢复"时误判为 0；
同时横道分项就是"全局模式关闭后是否真回了原版"的直接证据，日志行 `Restore audit: ... crosswalk subset N entries on M lanes`。

**文案**：`mode.label` 简中「作用范围」→「避让范围」、繁中「作用範圍」→「避讓範圍」
（其余语言仍是各自的习惯译法，未动）；README 选项表同步改名，并补一句
"有信号灯的路口仍会因红灯停车，那是原版信号灯逻辑，不是让行人"。

构建 0 错误 0 警告，Burst 三平台 **7 methods**，已部署本地 Mods 目录。**未发布**。
待实机验证：① 全局模式下无灯横道车辆直接穿过；② 全局模式下 F3 关闭 → 横道恢复让行
（看 `Restore audit` 的 crosswalk subset）；③ 全局→出入口切换 → 横道与范围外道路都恢复；
④ 出入口模式下横道仍然让行（v0.5 语义不变）。

**独立复核补漏（同会话，代码审查 agent 查状态机后修）**：
1. **审计只在关闭态推进**：原来 `TickRestoreAudit()` 挂在 `OnUpdate` 最前，启用态也会推进。
   关闭→再启用间隔小于 `kRestoreAuditDelayFrames` 时，审计会在我们已重删之后测出 0 →
   误判"没恢复" → 白跑一次全城 owner+lane `Updated`（正是 v0.7.2 干掉的那种 churn）+ 假告警。
   现挪进 `!enabled` 分支，并在 关闭→启用 跳变时撤销挂起的审计。
2. **范围收窄后旧活跃集会继续作恶**：事件驱动重刷 job 无条件读 `m_VehicleSets[active]`，
   而收窄时该集合仍是旧范围的成员 → 越界车道在集合重建的那几帧里会被再次删掉，
   刚还原的成果被抵消。新增 `m_SetsStale`：范围/模式改变或关闭时置位，
   下一轮集合起始处（那里已 `Dependency.Complete()`，无在飞 job）把两侧一起清空，
   过渡期判定退化为"类型 + 锚点邻域"。
3. **交换帧的并发句柄竞态**：`AdvanceAccessSets` 的交换分支直接 `Clear()` 旧活跃集并读
   新活跃集的 `Count`，而上一帧的 `BuildAccessSetsJob`（写新集）/重刷 job（读旧集）可能还在飞
   → `NativeHashSet` 并发句柄未释放就直读，开发版会抛 `InvalidOperationException`。
   与轮次起始处同样补 `Dependency.Complete()`（每 2048 帧一次，成本可忽略）。
4. **全局模式下改 Include\* 不算收窄**：全局分支 `IsAccessLaneByType` 压根不看 Include\*，
   原来仍会触发"全城重建 + 6 帧空窗"。`narrowed` 现在要求新模式是出入口（或 global→access 本身）。
   未采纳的两条：审计以"全城只要有 1 条行人条目就算恢复"为判据（宁可宽松，避免升级抖动）、
   `chunkCount<=0` 直接跳过审计（没有车辆车道就没有被删的东西，语义平凡成立）。

复核确认无误的点（记录以免下次重查）：横道开关三处调用一致（全局全量删 1、事件重刷 `globalMode?1:0`、
出入口集合批删恒 0 且该 job 由 `!globalMode` 把守）；`m_InitialGlobalApplyDone` 在关闭与范围改变两处都重置，
故 关闭→再启用、access→global 都会重跑含横道的全量删；所有 job 调用点字段齐全，
`m_AccessVehicleSet = default` 的分支不会读到它；审计数组每条返回路径都 Dispose，不会死循环。

#### ⑦ v0.7.8：出入口模式"关不掉"——恢复判据被横道条目糊弄（2026-09-19）

**用户实机反馈**：「帧数确实没怎么降低，全局模式下关闭也能恢复原版避让了，
但**仅出入口模式下又没办法恢复了，只要开启过就永远不会避让**」。

**决定性证据来自玩家自己的模组日志**（`Logs\AccessAnarchy.AccessAnarchyMod.log`，
2026-09-19 一次会话 7 轮开关，v0.7.7 全都在报 `Restore audit: ... vanilla avoidance restored`）：

| 关闭时刻 | 审计报出的行人条目 | 其中横道 | 非横道 | 距关闭的墙钟 |
| --- | --- | --- | --- | --- |
| 15:36:51（global） | 96003 | 71915 | 24088 | 486ms |
| 15:38:44（global） | 96003 | 71915 | 24088 | 602ms |
| 15:39:51（access） | 96003 | 71915 | 24088 | 375ms |
| **15:41:33（access）** | **73063** | **71915** | **1148** | **16ms** |
| **15:43:26（access）** | **73063** | **71915** | **1148** | **15ms** |
| 15:44:17（access） | 96003 | 71915 | 24088 | 602ms |
| 15:44:53（access） | 96003 | 71915 | 24088 | 592ms |

读法：出入口模式**从不删横道条目**，所以 71915 条横道条目在"完全没恢复"的态里也照样在场。
那两行 73063 = 横道 71915 + 非横道 1148，就是**仍然被我们剥离**的原样（正常应是 24088），
可 v0.7.7 的判据是 `lanes > 0`（"还有行人条目就算恢复"）→ 判成成功 → 既不重试也不报失败。
日志里唯一可疑的相关变量是「距关闭的墙钟」：两次异常都只有 15-16ms，正常都是 375-600ms。
机制解释：本系统在 `GameSimulation`（一帧渲染内可连追多步模拟），原版 `LaneOverlapSystem`
在 `MainLoop` 的 `Modification4B`（一渲染帧一趟），**只等 6 个模拟帧有可能在它一次都没轮到的窗口里就测完**。

**v0.7.8 三处修法**：
1. **判据换成"这个模式下我们一定删过的那一类"**：出入口模式看 `非横道条目 > 0`，全局模式看 `横道条目 > 0`；
   另加总量闸：启用后第一遍（帧末 ECB 还没回放、尚未删过任何东西）扫一次存成
   `m_BaselineEntries/m_BaselineCrosswalk` 基线，恢复后的条目数须 ≥ 基线 8 成（留 2 成给期间修路拆路）。
2. **不达标就多轮加力重试**：`kRestoreCheckDelays = {30, 90, 240, 600, 1200}` 模拟帧，最多
   `kRestoreMaxChecks = 5` 轮；第 1 轮再推一次 `m_Loaded`，第 2 轮起**同时**补打
   owner+lane `Updated`（两套机制交替，防单点被游戏吞掉）。每轮都打 `Restore check #N ... vs baseline ...`。
3. **范围收窄后的重删等待也从 6 帧提到 120 帧**（`kReapplyDelayFrames`）：同一个时窗陷阱——
   等太久不动的话会"我们先重删、原版随后重建把删除冲掉"，表现成"切了范围没反应"。
   代价：切换避让范围后约 1-2 秒才收敛。

**仍未完全排除的可能**（下一轮日志可直接分辨）：若 `Restore check` 报 `NOT restored` 且五轮后仍
`classBack=false`，说明原版重建真的没落地 → 下一步只剩"自己在关闭时把备份条目写回"这条确定性路线
（需要维护 lane→被删条目 的备份，慎防自定义组件进存档）。若报 `restored` 而玩家仍看到不避让，
说明数据回了但行为另有通道（要回头查 `LaneObject` 侧与 `CarLaneFlags`）。

构建 0 错误 0 警告，Burst 三平台 **7 methods**，已部署本地 Mods 目录。**未发布**。
待实机验证：出入口模式下 开→关，日志应出现 `Baseline pedestrian overlaps: ...`（开启时）与
`Restore check #1 (access-zones): ... non-crosswalk 2xxxx ... vanilla avoidance restored`；
若出现 `NOT restored (classBack=false ...)` 后面还会自动重试，把整段发我。

### 0.5 v0.7.1 修订（快捷键名称丢失 + 帧数优化收口，2026-09-15）

**用户实机反馈（v0.7.0）**：① 快捷键设置中快捷键名称都没了；② 帧数好像并没有优化成功，不确定是不是行人碰撞引起的。

#### ① 快捷键名称丢失——根因与修法

**根因（与 BLG 交接文档同款坑）**：选项页渲染 `ProxyBinding` 属性那一行的标题/说明，用的是
`GetOptionLabelLocaleID(nameof(属性))` / `GetOptionDescLocaleID(nameof(属性))`
（→ `Options.OPTION[id.Setting.ToggleEnabledBinding]`）。
v0.7.0 误用了 `GetBindingKeyLocaleID(actionName)`（→ `Options.OPTION[id/action/Press]`），
那是**输入系统动作名**，不是设置页那一行的标签 → 界面上键位行有、名称空白。

**修法（照抄 BLG v1.0.3 L10n.cs L1081-1084）**：
- `LocaleTable.Build` 改为注册：
  - `GetOptionLabelLocaleID(nameof(Setting.ToggleEnabledBinding))` → `hotkey.enabled`
  - `GetOptionDescLocaleID(nameof(Setting.ToggleEnabledBinding))` → `hotkey.enabled.desc`（新增）
  - 同理 ToggleScopeBinding
  - 保留 `GetBindingMapLocaleID()` → `bindingMap`（输入映射分组名）
- 绑定属性补 `[SettingsUISection(kSection, kGroupMain)]`，归入「行为」组。
- 12 语言各补 `hotkey.enabled.desc` / `hotkey.scope.desc`。

**反编译证据**：`Game/Modding/ModSetting.cs` L308-316（GetOptionLabel/DescLocaleID）、
L343-370（GetBindingKey/MapLocaleID）；BLG `releases/v1.0.3/game-sources/L10n.cs` L1079-1084。

#### ② 帧数没优化成功——三个残留热点

v0.7.0 的邻域集合思路对，但 A 干预点**没有真正变成 O(邻域)**：

1. **A 仍对全城 chunk 做 `ScheduleParallel(m_OverlapQuery)`**：集合过滤只跳过 overlap 扫描，
   迭代本身仍是 O(全城)。57km 地图几万条车道，每 4 帧一次，这就是「优化了还是卡」的主因。
2. **`AdvanceAccessSets` 同帧被调两次**（`!globalMode` 一处 + `!globalMode || bloodEffects` 一处），
   白做一倍候选推进，且可能连续两次 `Dependency.Complete()`。
3. **撞击反馈开启时**：邻域每个行人每帧做移动对象四叉树查询（半径 2.5m），
   行人多时显著加压。用户问「是不是行人碰撞引起的」——若 BloodEffects 开着，是。

**v0.7.1 修法**：
| 热点 | 修法 |
|---|---|
| A 全城迭代 | access 模式改为 `StripAccessOverlapsFromSetJob`：只遍历车辆邻域集合；`BuildAccessSetsJob` 扩展为同时收「类型出入口」（Garage/Connection/Area/建筑自有）+「邻近引道」。全局模式仍走 `ScheduleParallel` 全城（本来有效） |
| 双次 AdvanceAccessSets | 合并为每帧一次；`bloodEffects` 提前读取 |
| 血迹检测频率 | `kBloodInterval = 4`：检测每 4 帧一次（踉跄动画 240 帧，4 帧延迟不可感知） |
| 集合构建步长 | `kSetBuildStep` 512 → 1024（重建更快） |

**构建**：0 警告 0 错误；Burst 三平台编译 6 methods（含新 `StripAccessOverlapsFromSetJob`）；
已部署 `Mods\AccessAnarchy\`。**未发布**。

**待实机验证（用户本地测）**：
1. 选项页快捷键行显示「启用/关闭模组」「切换避让范围」（中文）或对应语言标签，不再是空白；
2. F3/F4 仍可按、改键可持久化；
3. 57km 地图 access 模式帧数应接近原版（不再因本模组 O(全城) 掉一半）；
4. 出入口穿透 + 横道避让保持（v0.7.7 起：仅出入口模式保持横道，全局模式横道也穿透）；全局模式仍生效；
5. ~~BloodEffects 开/关对比~~ → **v0.7.3 已删除**（用户实机确认踉跄从未触发）。

### 0.0 对上一会话（Percho agent）的查验结论——重要，防止再被误导

`v0.7续接提示词.md`（上一会话 native compaction 前生成）里的「磁盘真实状态」「性能方案」「快捷键方案」**全部是幻觉**：它描述的 OnGameViewInput / FindObjectsByType&lt;NetInfo&gt; / GUILayout 手写面板 / IsBuilding-IsRoad bool / AssemblyInfo.cs / -p:CitiesGameLibsDir 在本项目**从来不存在**——那是 Cities: Skylines 1 代的模组模式，本项目是 CS2 ECS + 官方 ModSetting 声明式 UI。照它实施会破坏已验收的 v0.6.0。**已丢弃其 v0.7 设计**；其真实有效的产出（平台元数据回填 commit 6954e62、防覆盖截图回填、平台 API 直查方法、媒体备份）全部保留并入库。F3/F4 用户选择保留。

### 0.1 性能优化（需求 1，57km 地图帧数减半的根因与修法）

**根因（v0.6.0 架构内的真实热点）**：
- 干预点 C 每帧对【全城】行人道（大地图数万条）做 chunk 迭代 + 每实体 Curve 查找 + 3×3 网格查询；
- 干预点 A 每 4 帧对【全城】车辆车道做同样判定 + 每条 overlap 条目 2 次组件查找。

两者都是 O(全城)，随城市规模线性恶化。

**修法（行为不变）**：
1. **邻域集合**：每轮把「邻近锚点的行人道/车辆车道」收进 NativeHashSet（双缓冲 A/B 轮换）。分帧增量构建：每帧主链上处理 512 个候选（IJob BuildAccessSetsJob），全城几万条约 1-2 秒完成一轮，swap 后新集合生效；构建期间判定用上一轮集合（无空窗）。候选收集每轮一次（两个查询 ToEntityArray，Dependency.Complete() 后同步做）。
2. **C 改单线程 IJob 直接遍历行人道集合**（几百条），省掉全城 chunk 迭代。
3. **删除干预点 B**（StripPedestrianRegistrationsJob）——v0.3 实机日志（registration lanes=0）证明连接道没有 LaneObject buffer，它每帧空转。
4. **全局模式跳过 C**（A 已删全部车辆车道 overlap 的行人条目，C 纯冗余）+ 跳过集合构建。
5. A 保留 IJobChunk 形态：access 模式下每实体先查集合（一次哈希查询）再进重活；保留 Crosswalk 排除、保留每帧 C（ECB 帧末回放 + CarNav 帧中读取的时序约束不变）。

优化后每帧成本（access 模式）：512 次候选判定（≈30μs）+ 邻域行人道几百条的 buffer 扫描（≈30μs）+ A 每 4 帧一次的全城 Contains 遍历（≈0.2ms/4帧）——与城市规模基本无关。

### 0.2 快捷键（需求 2，F3=启用/关闭，F4=切换避让范围）

**API 事实（全部反编译核实，直接照抄）**：
- `[SettingsUIKeyboardAction(name)]`（Game.Settings，class 级，AllowMultiple）声明动作；usages 传 null = Usages.DefaultSet 全集（菜单+游戏内都响应）——不需要自造 usage 字符串。
- `[SettingsUIKeyboardBinding(BindingKeyboard.F3, actionName)]` 挂在 ProxyBinding 属性上；BindingKeyboard 枚举含 F3/F4。map = ModSetting.id（模组自己的 map，不存在 "gameactions" map）。
- Mod.OnLoad 顺序硬约束：**LoadSettings 必须先于 RegisterKeyBindings**（binding 属性值先从 KeybindingSettings 恢复玩家的改键，注册时才不会顶掉）→ RegisterKeyBindings() → GetAction(name) → shouldBeEnabled = true → onInteraction += (action, phase)，用 **InputActionPhase.Started** 过滤按下沿（枚举在 UnityEngine.InputSystem）。
- csproj 需补引用 Unity.InputSystem.dll（InputActionPhase 所在程序集）。
- 切换动作：改 Instance 属性 + ApplyAndSave()（Game.Settings.Setting public 方法）落盘。
- 12 语言新增 3 词条/语言：hotkey.enabled / hotkey.scope / bindingMap（GetBindingKeyLocaleID ×2 + GetBindingMapLocaleID 的字典映射）。

### 0.3 v0.7.0–v0.7.8 状态

- **v0.7.8**（2026-09-19）：出入口模式"关不掉"——玩家日志证明 v0.7.7 的恢复判据 `lanes>0` 被
  横道条目（出入口模式从不删横道）长期糊弄，重建没落地也报"已恢复"、不重试。
  现判据改为"本模式下我们一定删过的那一类条目回来了没有"+ 与启用时基线比总量（≥8 成），
  不达标按 30/90/240/600/1200 帧最多 5 轮交替重试两套机制（m_Loaded / owner+lane Updated）；
  范围收窄后的重删等待 6→120 帧（同一时窗陷阱）。详见 §0.7⑦。
  构建 0 错误 0 警告，Burst 三平台 7 methods，已部署本地；**实机通过（2026-09-19），已发布平台**（见「发布记录（v0.7.8）」）。
- **v0.7.7**（2026-09-19）：① 全局模式（全部道路）不再排除人行横道——`m_IncludeCrosswalks` 按模式取值，
  与 12 语言 `mode.desc`「包括人行横道」的既有文案对齐；出入口模式保持 v0.5 的横道保留。
  ② 审计增加横道分项（`crosswalk subset`），作为"关闭后是否真回原版"的直接证据。
  ③ 设置项 `mode.label` 简中/繁中改名「避让范围」/「避讓範圍」，README 同步 + 补信号灯说明。
  构建 0 错误 0 警告，Burst 三平台 7 methods，已部署本地；**未发布**。（详见 §0.7⑥）
- **v0.7.6**（2026-09-18）：开关失效修复 v2——原版 overlap 重建的闸门与工作清单都按**拥有者实体**
  （`SubLane` 缓冲：edge/node/building）算，v0.7.5 只给车道打 `Updated` 因此完全空转。
  现改为：主路径反射置 `LaneOverlapSystem.m_Loaded=true` 走读档同款全量重建，兜底「全部拥有者 + 全部车道」
  打 Updated，外加一次审计日志；范围收窄（global→access / 取消勾选）同样先还原再按新范围重删（见 §0.7⑤）。
  构建 0 错误 0 警告，Burst 三平台 7 methods，已部署本地；**未发布**。**实机：关闭恢复与收窄还原均已生效**（2026-09-19）。
- **v0.7.5**（2026-09-16）：修复开关失效——关闭时给全城车辆车道打 `Updated`，触发原版 LaneOverlapSystem 重建 overlap、恢复行人避让（见 §0.7④）。构建 0 错误 0 警告，Burst 三平台 5 methods，已部署本地；**未发布**。**实机证明无效**（见 §0.7⑤）。
- **v0.7.4**（2026-09-16）：事件驱动重刷——用 `Updated` query 替换 120 帧定时器，修路后当帧恢复（见 §0.7③）。构建 0 错误 0 警告，Burst 三平台 4 methods，已部署本地；**未发布**。
- **v0.7.3**（2026-09-16）：删除撞击反馈 BloodEffects（用户实机确认无效，见 §0.7①）。连带移除 ped set / 四叉树 / Stumbling 管理代码。
- **v0.7.2**（2026-09-15）：性能架构重建——去掉每帧 LaneObject 写战，A 降频至 120 帧，集合整轮重建（见 §0.6）。构建 0 错误 0 警告，已部署本地；**未发布**。
- **v0.7.1**（2026-09-15）：修复快捷键名称 + 性能热点（见 §0.5）。构建 0 错误 0 警告，Burst 三平台 6 methods（含 `StripAccessOverlapsFromSetJob`），已部署本地 Mods 目录；**未发布、未触碰 Paradox 平台**（用户要求）。
- v0.7.0 构建曾 0 错误 0 警告，已部署本地；**未发布**。
- 待实机验证：帧数改善幅度、F3/F4 触发、**选项页键位名称显示**、横道避让保持、改键持久化。
- v0.7续接提示词.md 作为反例存档入库（tools/ 里上一会话的 API 调研 txt 同步入库）。

---

### 0.4 撞击反馈功能（v0.7 增量，2026-09-15）→ **v0.7.3 已删除**

> **v0.7.3 删除通知**：用户实机确认踉跄动画从未触发，功能及设置项已整体移除。以下为原始调研记录，仅供参考。

**需求原文**：穿过行人后加一个血迹飞溅效果，可选项。

**调研结论（为什么不是真血迹）**：
- 检索 Game.dll 全部类型与 Prefabs/Effects 目录：**游戏没有任何血液粒子/VFX/贴花资产**（PEGI 低分级决定 Colossal 不会做血腥内容）。
- DecalProperties 只是 RenderPrefab 的贴图/层属性——贴图与网格是 Unity 资产，纯代码模组无法凭空生成。
- 正常车-人重叠在游戏里**没有任何事件**（Impact 只来自随机事故系统，见 §1.3），"穿过时刻"必须自己检测。

**落地实现（原生"被撞"表现，零模拟副作用）**：
1. **检测**：StripPedestrianOnNearbyWalkwaysJob（C job）在删除邻域行人注册前，对每个行人
   （仅 Human）做移动对象四叉树半径查询（Game.Objects.SearchSystem.GetMovingSearchTree，
   BloodHitIterator：排除自己/停靠车（ParkedCar），1.5m 内存在 Vehicle 判定命中）。
2. **触发**：命中 → ECB AddComponent<Stumbling>（Game.Creatures.Stumbling，空标记组件）——
   行人进入原生"被撞踉跄"动画；同时记录到 NativeHashMap<Entity, uint>（到期帧 = 当前+240）。
3. **生命周期**：游戏内**没有任何系统移除 Stumbling**（全局 grep 零命中；原生 Impact 链靠
   伤亡流程销毁行人结束踉跄）→ RemoveStumblingJob 每帧收集到期键并 ECB RemoveComponent。
   主线程不读在途 job 的 map.Count（数据竞争）——bloodEffects 开启期间无条件调度
   removeJob（空 map O(1) 早退）。
4. **已踉跄排除**：map.ContainsKey + ComponentLookup<Stumbling> 双查，防止每帧重复触发。
5. **范围**：仅出入口邻域（检测依赖邻域行人道集合）；全局模式下集合仍构建（供检测）、
   删除逻辑（DoStrip）跳过。全局模式不覆盖全城检测——性能优先。
6. Setting.BloodEffects（默认关闭，血腥内容惯例）+ 12 语言词条（blood.label/blood.desc，
   描述里如实说明"游戏无血液资产，以原生踉跄呈现"）。

**新增 API 事实**：
- Game.Creatures.Stumbling：空 struct，唯一游戏写入者是 ImpactSystem（L206-208，行人分支
  无条件 AddComponent）；HumanNavigationSystem 的 chunk.Has(m_StumblingType) 分支
  （UpdateStumbling，L546/627）驱动踉跄行为：导航目标=当前位置、path 置 obsolete。
- csproj 新增引用：Colossal.Collections（NativeQuadTree/IUnsafeQuadTreeIterator——
  注意 NativeQuadTree.Iterate<TIterator> 要求同时实现 INativeQuadTreeIterator 与
  IUnsafeQuadTreeIterator 两个接口，只实现后者会 CS0315）。
- NativeHashMap<,> 的枚举器类型是 NativeHashMap<,>.Enumerator（不是 NativeParallelHashMap 的）。
- Game.Objects.SearchSystem.GetMovingSearchTree(readOnly, out deps) 可在自建系统里只读访问
  移动对象四叉树（与 CarNavigationSystem 同款用法），deps 并入自己的 Dependency 链。

**若用户坚持要真血迹**：需要走资产模组路线（自带 VFX/贴图资产包，Modding Toolchain 资产管线），
纯代码模组在此到顶了。

## 0b. v0.6 修订（UI 文案：模组名定稿 + 下拉框语言跟随，先读这节）

**用户反馈（功能已验收通过）**：① 模组名统一纯英文 `Access Anarchy`（Anarchy 不好翻译，zh-HANS/HANT 的括号后缀删除）；② 作用范围下拉框只显示简中，切语言不变。

**②的根因（框架机制，反编译核实）**：`DropdownField.Update()`（decompiled/Game/UI/Widgets/DropdownField.cs L62-74）只在 `itemsVersion` 变化时才重新调 `GetModeItems`；`itemsVersion` 来自 `SettingsUIValueVersionAttribute`（AutomaticSettings L510-518, L1268）——**没有该特性的字段版本恒为 0，items 建页时取一次就永不刷新**。GetModeItems 里"实时读语言"的代码本身没错，只是框架根本不再调用它。

**修复**：`Mode` 属性加 `[SettingsUIValueVersion(typeof(Setting), nameof(GetModeItemsVersion))]`，`GetModeItemsVersion()` 返回 `activeLocaleId.GetHashCode()`——语言切换 → 版本变化 → 框架重取 items。这是比 BLG 交接文档 §56/58「控件值建页时抓走」更精确的同族机制：**下拉框 items 的刷新必须显式给版本信号**。

**v0.6 已核验**：`GetModeItemsVersion`、`SettingsUIValueVersion` 均进 DLL；中文括号字样（`出入口无政府）`）已从 DLL 消失。

## 0b. v0.5 修订（横道误伤修复）

**实机反馈**：v0.4 出入口穿透达成，但**人行横道也不避让了**。根因：v0.4 的 A/C 干预点按「距锚点 32m」判定，而出入口与横道在城市里天然紧邻——横道（crosswalk）本身就是 PedestrianLane，被 C 摘了行人注册、被 A 删了引道车道的 overlap 条目。

**修复（v0.5）**：横道在数据层有精确标志——`PedestrianLaneFlags.Crosswalk`（HumanNavigationSystem.GetTargetSpeed 与 SecondaryLaneSystem 都用它区分横道）。
- A（overlap 删除）：对端行人道带 Crosswalk flag → 条目保留（车辆继续给横道让行）；行人连接道条目照删（ConnectionLaneFlags 没有 Crosswalk 概念）。
- C（行人注册删除）：带 Crosswalk flag 的 PedestrianLane 整条跳过。

**方案取舍记录**（用户问「做出入口无碰撞 vs 让行人避让车辆」）：选择继续「出入口无碰撞」+横道精确排除，理由：
1. 需求 2.1.3 明确「行人行为不受影响」，非目标范围明确保留横道避让——横道误伤是 bug 不是 trade-off；
2. 「行人避让车辆」在 CS2 里不是恢复某个被关掉的行为——行人 AI（HumanNavigationSystem 的 UpdateNavigationTarget/GetTargetSpeed，Burst）本来就不避让移动车辆，只对信号灯（LaneSignal.Stop/SafeStop → 行人停）、静态障碍、其他行人反应。要实现它要么重写行人 AI（系统替换，几千行 Burst 逻辑），要么给行人 currentLane 动态塞 LaneSignal——行人连接道是行人动态注册的、川流不息，信号的管理又是新的时序战场，且 Stop 信号会让行人无限堆积在 driveway 前，造成比车等人更糟的新堵塞点；
3. Anarchy 哲学：数据编辑最小化。Crosswalk 排除只是两处 flag 检查。

**v0.5 待实机验证**：出入口穿透保持 + 横道避让恢复。若「横道恢复但出入口失效」，说明测试场景的出入口与横道重叠（生效路径依赖横道数据），需要用户描述具体测试场景再调（候选：横道半径排除改为距离加权、或仅在行人连接道范围内生效）。

## 0b. v0.4 修订（实机日志驱动的空间判定）

**实机日志决定性数据**（`Logs\AccessAnarchy.AccessAnarchyMod.log`——注意 Colossal LogManager 的 logger 各自写 `Logs\<logger名>.log`，Modding.log 只有框架行）：

```
AccessZoneOverlapSystem heartbeat: pass N, 4836 overlap lanes / 0 registration lanes, mode=access-zones
```

**`0 registration lanes` 证明**：全城的连接道（ConnectionLane/GarageLane/AreaLane）**没有任何 LaneObject buffer**——连接道 prefab 资产只有 `Game.Prefabs.ConnectionLane` 组件（其 `GetArchetypeComponents` 只贡献 `Game.Net.ConnectionLane`，反编译核实），没有 CarLane/PedestrianLane 组件（那两个组件类才贡献 LaneObject+LaneOverlap buffer）。由此闭环：

1. 连接道没有 LaneObject buffer → 行人不会注册在连接道上（CheckChanges 走搜索树分支）；
2. 连接道没有 CarLane 组件 → `CarLaneSpeedIterator` 对它完全不检查；
3. 连接道没有 LaneOverlap buffer → 无 overlap 可删；
4. **连接道自身不存在任何可干预数据**——v0.2/v0.3 在连接道层面的干预全部无效。

**实机观察的"出入口避让" therefore 发生在【出入口紧邻的普通马路车道（引道）与行人道的 LaneOverlap】上**：行人横穿入口处的人行道/横道（注册在人行道上），车辆在引道马路车道上经 overlap 读到行人减速。这类车道是普通 CarLane，任何"按车道类型"的判定都覆盖不到——全局模式生效（全删）与此完全自洽。

**v0.4 解法 = 空间判定（锚点网格）**：
- `BuildAnchorGridJob`（每 512 帧，单线程）：遍历连接道/GarageLane/AreaLane，取 Curve 五个控制点写入 `NativeHashMap<int2, FixedList128Bytes<float2>>`（32m 网格）。
- 判定 `IsNearAnchor`：车道的 a/d/中点查 3×3 邻域格 + 32m 距离校验。
- **A 扩展**（每 4 帧）：邻近锚点的普通车辆车道 → 删其 LaneOverlap 的行人条目（覆盖引道↔人行道/横道）。受 IncludeConnections 开关控制。
- **C 新增**（每帧）：`StripPedestrianOnNearbyWalkwaysJob`——邻近锚点的 PedestrianLane → 删其 LaneObjects 里的行人与动物。横穿出入口区域的行人无论注册在哪条行人道上都被摘除（釜底抽薪）。全局模式下处理全城行人道。
- **B 保留**（每帧）：连接道类兜底（实测 0 条，防 mod 资产引入带 buffer 的连接道）。
- 行人带 `Game.Creatures.Creature` 组件已确认（`Game.Creatures.InitializeSystem` 查询 `ReadOnly<Creature>()` 同时处理 Human/Animal）。
- **ECB 回放滞后一帧的时序**（v0.3 教训）：删除命令帧末回放、CarNav 帧中读取 → CarNav 每帧读到的是"上一帧末删除后"的状态 → B/C 必须每帧执行，缺席任何一帧车辆就会看到重加的行人。

**v0.4 待实机验证**：锚点网格是否覆盖用户测试场景。若仍有个别出入口不生效，把日志 heartbeat 里的 anchors 数量与具体位置发来；半径 32m（`kAccessRadius`）可调。

#### 0.1.1 本地化：12 份字典全被填成同一语言（v0.3）

**症状**：游戏切英文仍显示中文。**根因**：`LocaleTable.Build(setting, locale)` 内部用 `T(key)` 取词，而 `T()` 读的是全局静态 `_activeLocale`（OnLoad 时被设为当时的游戏语言），与传入的 `locale` 参数无关——于是 12 份 `AddSource` 字典全部填成了同一语言。**修复**：`Build` 改为按传入 locale 取各自词条表（`d["..."]`）。教训：**AddSource 的每份字典必须各自构建，任何"当前语言"状态只允许用于 dropdown displayName 这类运行时取值**。dropdown 选项名由 `GetModeItems()` 实时读 `GameManager.instance.localizationManager.activeLocaleId`（只在建页/刷新时调用，非热路径）。

### 0.2 "仅出入口"模式失效：B 干预点的删除节奏跟不上重加

**根因是 ECB 回放滞后一帧 + 删除间隔 4 帧**。精确时序（v0.2，每 4 帧）：

```
帧 0:  job 读 buffer（= 帧-1末回放状态，行人条目在）→ 记录 SetBuffer → CarNav 读（脏，减速）
       → 帧末回放（删除生效）
帧 1:  不跑 → CarNav 读（干净 ✓）→ HumanNav 切片重加
帧 2:  不跑 → CarNav 读（脏，减速！）
```

4 帧里 3 帧可见 → 表现为"完全没生效"。**修复**：B 改为**每帧**执行——每帧都记录删除命令，CarNav 每帧读到的都是"上一帧末已删除"的状态，行人永远不可见。同时把 B 的查询从全城车辆车道收窄为 `Any=[GarageLane, ConnectionLane, AreaLane]`（出入口核心车道，量小，每帧成本可忽略）；普通道路车道继续由 A 管（overlap 删除的重加来源是 LaneOverlapSystem 的车道重建，频率低，每 4 帧够用——全局模式实测有效）。

**顺带确认**：行人实体确实带 `Game.Creatures.Creature` 组件（`Game.Creatures.InitializeSystem` 的 `m_CreatureQuery = GetEntityQuery(ReadOnly<Creature>(), ReadOnly<Updated>())` 同时处理 Human/Animal），B 的删除判据成立。

**残余风险（下一轮实机验证点）**：若每帧删除后仍有出入口避让，下一个嫌疑是"行人横穿 driveway 时的 currentLane 是长段人行道而非行人连接道"（B 刻意不删人行道注册以免破坏马路避让），届时改按建筑包围盒做空间判定。

---

## 0b. v0.2 修订（第一轮实机反馈驱动的机制修正）

**实机结论**：全局模式生效（马路上的车不让人了），"仅出入口"模式完全无效。由此反推出 v0.1 调研的一处关键错误并已修正：

1. **连接道（driveway/garage）根本不存在 LaneOverlap 条目可删**。证据链：
   - 所有建筑连接道都带 `SecondaryLane` 组件（`Game.Buildings.RoadConnectionSystem.cs` 1334/1438 行无条件 AddComponent）；
   - `LaneOverlapSystem.UpdateOverlaps` 对 `m_SecondaryLaneData.HasComponent(subLane)` 的车道直接跳过（写入侧排除，decompiled/Game/Net/LaneOverlapSystem.cs 1043 行），`CheckOverlaps` 的对端也排除（1116 行）；
   - LaneOverlap buffer 只来自 archetype：`Game.Prefabs.CarLane`/`Game.Prefabs.PedestrianLane` 组件的 `GetArchetypeComponents` 贡献 `LaneObject`+`LaneOverlap`（全树无 `AddBuffer<LaneOverlap>`，唯二来源）；而 `Game.Prefabs.ConnectionLane` 组件只贡献 `Game.Net.ConnectionLane`——**连接道的 overlap buffer 永远是空的**。
   - 所以 v0.1 的"删除连接道 overlap"分支是 no-op，`IncludeConnections` 按 flags 过滤（Road|Track|Parking）的疑虑反而不是主因。
2. **车辆"看见"行人的唯一公共汇聚点是车道的 `LaneObject` buffer**：`CheckCurrentLane`（本车道）与 `CheckOverlappingLanes`（经 overlap 指向的对端车道）最终都读某个车道的 LaneObjects 找带 `Game.Creatures.Creature` 的对象。行人由 `HumanNavigationSystem.CurrentLaneCache.CheckChanges` 注册到**自己 currentLane** 的 LaneObjects（research/Game.Simulation.HumanNavigationHelpers.cs 54-91 行）。
3. **v0.2 干预点 B**：新增 `StripPedestrianRegistrationsJob`——删除"出入口区域车道"（GarageLane / 任意 ConnectionLane / AreaLane / 建筑自有 CarLane-ParkingLane）LaneObjects 里的行人与动物条目。无论车辆通过哪条路径读行人注册，读的都是这份 buffer——釜底抽薪。原 overlap 删除（干预点 A）保留，它管马路上的行人横道场景（实机已验证有效）。
4. **`GarageLane` 组件只在创建 Parking 连接道时由 RoadConnectionSystem 附加**（1439-1442 行），是连接道的可靠标记之一。
5. AreaLane 内部（`CarCurrentLane.m_LaneFlags & Game.Vehicles.CarLaneFlags.Area`）车辆本就只对目标点减速、不查行人（CarNavigationSystem 1573/1910-1958 行：Area 分支用 VehicleCollisionIterator 且它 skip Creature）——停车场内部天然穿透，与 v0.1 结论一致。

v0.2 已知残余风险（待实机复验）：若行人横穿 driveway 时的 currentLane 既不是行人连接道也不是出入口车道（例如长段人行道横跨整个入口区），B 不会删它在人行道上的注册（刻意不删，避免破坏马路避让）——此时该行人仍会让车停。实机若再现，考虑按"建筑包围盒邻近"做空间判定。

---

## 1. 核心调研结论：车辆避让行人的机制链

### 1.1 唯一动态机制 = CarLaneSpeedIterator.CheckPedestrian

`Game.Simulation.CarNavigationSystem.UpdateNavigationJob`（Burst 编译，每帧对 1/16 切片的车辆运行）调用 `Game.Simulation.CarLaneSpeedIterator`（struct，反编译件 `research/Game.Simulation.CarLaneSpeedIterator.cs`）：

1. `IterateFirstLane(...)`（118 行起）：对车辆当前车道，若是 `CarLane`（196 行起还有 `PedestrianLane` 分支），调用
2. `CheckCurrentLane(...)`（1103 行）：遍历**本车道** `LaneObjects` 缓冲（同车道对象，行人乱穿马路时走到这里）；
3. `CheckOverlappingLanes(...)`（1172 行）：遍历本车道 `LaneOverlap` 缓冲，对每条 overlap 把 `m_Lane` 切到对端车道，读**对端车道的 LaneObjects**（1324 行起）：
   - 对端车道对象带 `Game.Creatures.Creature` 组件 → `CheckPedestrian(...)`（1346-1348 → 1403-1431 行）→ 按刹车距离压低 `m_MaxSpeed`，`m_BlockerType = Temporary`。**行人在车辆的曲线区间内时无条件减速（第一个 if 不成立直接落到底部），没有任何数据开关可以绕过。**
   - 不带 `Creature` → 走通用 `UpdateMaxSpeed`（车/静物避让）。

行人是怎么被看到的：行人行走时由 `Game.Simulation.HumanNavigationSystem`（每帧 1/16 切片）的 `HumanNavigationHelpers.CurrentLaneCache.CheckChanges` 把行人**注册到自己所在车道的 `LaneObject` 缓冲**（反编译件 `research/Game.Simulation.HumanNavigationHelpers.cs` 54-91 行：`buffer.Add(currentLane.m_Lane, entity, curvePos)`；不在车道上时注册进移动对象四叉树）。行人**永远不会被注册到车辆车道上**——车辆车道能"看到"行人，完全靠 `LaneOverlap` 条目指向行人道这一层引用。

> **结论 A（干预点）**：删除"车辆车道 ↔ 行人道"的 `LaneOverlap` 条目 = 车辆彻底看不到行人。这是唯一充分且最小的数据层干预点。
>
> **结论 B（Harmony 不可行）**：CheckPedestrian 在 Burst 编译的 job 内被内联，Harmony 无法拦截；CarNavigationSystem 约 3000 行逻辑，系统替换不现实。
>
> **结论 C（AreaLane 内已天然穿透）**：`Game.Simulation.VehicleCollisionIterator.CheckCollision`（research/Game.Simulation.VehicleCollisionIterator.cs 204-209 行）对带 `Creature` 组件的移动对象直接 `return`——只在 AreaLane（停车场区域车道）上迭代的它本来就跳过行人。

### 1.2 数据结构与重建时机

- `LaneOverlap`（`Game.Net.LaneOverlap`，buffer 挂在车道实体上，`[InternalBufferCapacity(0)]`）：`m_Other`（对端车道）、`m_Flags`、曲线区间字节、`m_PriorityDelta`。**双向记录**（A 有指向 B 的条目，B 也有指向 A 的），但车辆只读自己一侧的缓冲——只删车辆车道侧即可，不动行人道侧。
- `LaneObject`（行人/车辆的登记条目，含曲线位置）由各导航系统自行维护，与本模组无关。
- **重建时机**（`Game.Net.LaneOverlapSystem`，反编译件 research/decompiled/Game/Net/LaneOverlapSystem.cs 1893-2014 行）：**重建单位是「车道拥有者实体」而不是车道**——闸门是 `m_UpdatedOwnersQuery=[SubLane,Updated,!Deleted]` 非空（L1893/L1920），工作清单是这批拥有者（L1926），`UpdateLaneOverlapsJob` 按 `m_SubLanes[owner]` 逐条 `Clear()` 再重算（L1030-1035/L1077）。车道自己的 `Updated` 只喂 `m_UpdatedLanesQuery` 的 `CollectLaneDirectionsJob`/`UpdateLaneFlagsJob`（flags 两趟），**不触发重建**。世界加载后第一轮（`m_Loaded`→`m_AllOwnersQuery`+`m_AllLanesQuery`+`m_UpdateAll=true`）走全量。`UpdateLaneOverlapsJob` 会整体重写目标车道的 overlap 缓冲 → 本模组的删除会被冲掉 → 修路后必须重新应用（v0.7.4 用 Updated 事件驱动）。**这条口径 v0.7.5 曾写错成"车道带 Updated 即重建"，导致关闭开关的恢复完全空转，见 §0.7⑤。**
- overlap 缓冲**不序列化**（LaneOverlap 结构无 Serialize），存档重载后由 LaneOverlapSystem 重建，天然与本模组无持久化冲突。
- `Game.Net.FixLaneObjectsSystem` / `Game.Serialization.LaneObjectSystem` 只修 `LaneObject`，不碰 overlap。

### 1.3 碰撞事件与撞击

- `Game.Events.Impact` 组件的创建者只有 `Game.Events.InitializeSystem.InitializeTrafficAccident`（随机交通事故事件）和 `Game.Simulation.ObjectCollisionSystem`（只处理 `OutOfControl` 的失控车）。**正常车辆与行人模型重叠不产生任何撞击事件**——车辆模型穿过行人时没有物理碰撞、没有动画、没有事件。需求 4.2.2（无碰撞动画/事件）在删除减速逻辑后天然满足。
- `Game.Objects.LaneBlockSystem` 只处理带 `BlockedLane` 的静态物（垃圾桶等），与行人无关。
- `Game.Rendering.CameraCollisionSystem` 只管相机。

### 1.4 时序（Game.Common.SystemOrder.cs，反编译件 research/decompiled/Game/Common/ 下）

```
SystemUpdatePhase.GameSimulation:
  310: CarNavigationSystem            ← 读 LaneOverlap 决定避让
  311: CarNavigationSystem.Actions    ← 车辆自己的 LaneObject 注册
  ...
  343: HumanNavigationSystem          ← 行人的 LaneObject 注册（在 CarNav 之后！）
```

本模组系统注册为 `UpdateAt<AccessZoneOverlapSystem>(GameSimulation)` + `UpdateBefore<AccessZoneOverlapSystem, CarNavigationSystem>(GameSimulation)`。每 4 个模拟帧跑一次：删除（EndFrameBarrier 帧末回放）→ CarNav 读到无行人 overlap → HumanNav 下一帧的重新注册**不产生车辆车道侧条目**（见 1.1，行人只注册到自己的人行道）→ LaneOverlapSystem 偶尔重建的条目最多存活 4 帧。

### 1.5 出入口车道的判定（AccessZoneOverlapSystem.IsTargetLane）

| 类别 | 判定 | 反编译依据 |
|---|---|---|
| 车库坡道 | 车道实体带 `Game.Net.GarageLane` | 停车楼/车库出入口坡道 |
| 建筑出入连接道 | 带 `Game.Net.ConnectionLane` 且 `m_Flags & (Road\|Track\|Parking) != 0` | ConnectionLaneFlags（Road=0x20, Track=0x40, Parking=0x100）；行人连接道只有 Pedestrian=0x80 |
| 停车场内部行驶道 | 带 `Game.Net.AreaLane` | 挂在 Area 实体上的区域车道；`Game.Vehicles.CarLaneFlags.Area`（0x4000000）是**车辆 CarCurrentLane 的状态标志**，不在车道组件上（第一个坑，见 §3） |
| 建筑自有车道/停车位 | 带 `CarLane` 或 `ParkingLane`，且 `Owner` 链上所有者**不是** `Game.Net.Edge`/`Game.Net.Node`（即不是道路自有 → 不是路边停车位） | Owner（Game.Common.Owner，m_Owner 单跳） |
| 全局模式 | 上述全部（纯行人连接道跳过） | |

"对端是行人道"判定：`PedestrianLane` 组件存在，或（双保险）`ConnectionLane` 带 `Pedestrian` 标志且无车辆标志。

### 1.6 行人侧不读 overlap 的验证

行人行走（HumanNavigationSystem）与行人寻路（Game.Pathfind）均不读取 `LaneOverlap`；行人等信号灯走 `LaneSignal`（`GetTargetSpeed`，research/Game.Simulation.HumanNavigationSystem.cs 696-703 行）。删除车辆车道侧 overlap 对行人零影响。

---

## 2. 实现记录

- **零 Harmony**。csproj 不引用 Lib.Harmony（对比 BridgeTheLanguageGap 的发布产物少一个 0Harmony.dll）。
- 系统：`Systems/AccessZoneOverlapSystem.cs`（partial，源生成器要求）。Job `StripPedestrianOverlapsJob`（IJobChunk，Burst 编译成功——构建日志可见三平台 Burst 原生库生成）。
- **ECB 没有单条删除 buffer 元素的命令**（Entities 1.3 的 `EntityCommandBuffer.ParallelWriter` 无 RemoveBufferElement），用 `ecb.SetBuffer<LaneOverlap>(sortKey, entity)` 原子替换整个缓冲：保留全部非行人条目（车车 overlap 与优先级数据不动），行人对条目消失。记录前先 `HasPedestrianOverlap` 预检，无行人条目时零写入（幂等，避免每帧无谓的结构性变更）。
- 查询：`All=[LaneOverlap], Any=[CarLane, ParkingLane, GarageLane, ConnectionLane, AreaLane], None=[Deleted, Temp]`，`RequireForUpdate`。
- 选项：官方 ModSetting 框架（`[FileLocation]` + `AssetDatabase.global.LoadSettings` 落盘到 `ModsSettings\AccessAnarchy.coc`）；系统通过 `Setting.Instance` 静态引用在调度 Job 前读 getter（框架在主线程直调 setter，getter 恒为当前生效值，无需静态镜像——对比 BLG 交接文档 §10 #9 的场景：本模组读设置在主线程 OnUpdate 内，不是热路径）。
- 日志：首跑 + 每 1800 帧心跳（候选车道数、当前模式），日志键 `AccessZoneOverlapSystem`。
- 构建坑（与 BLG 交接文档 §10 一致的全部踩过）：
  - csproj 必须显式 `<Compile Remove="research\**;template\**">`，否则 2333 个反编译文件进编译（T3 教训的镜像）。
  - `RunModPostProcessor`/`RunModPublisher` 重声明 + `DOTNET_ROLL_FORWARD=Major` + `BeforeTargets="DeployWIP"`（§10 #23）。
  - IJobChunk 的 Execute 只有一个四参数签名（`in v128` 来自 `Unity.Burst.Intrinsics`），系统类必须 `partial`。
  - `Game.SystemUpdatePhase.GameSimulation`（枚举在根命名空间，不是 Game.Common）。
  - `EndFrameBarrier` 在根命名空间 `Game`。

---

## 3. 验证状态（诚实台账）

### 已验证（机器证据）

- T1 编译门禁：0 警告 0 错误，`AccessAnarchy.dll processing complete`，部署到 `<UserData>\Mods\AccessAnarchy\`（6 文件：托管 dll/pdb + 三平台 Burst 原生库）。
- T2 静态核验（node 双堆扫描，方法同 BLG verify.mjs）：类型 `AccessZoneOverlapSystem`/`StripPedestrianOverlapsJob`（#Strings UTF-8 堆）、首跑日志/心跳/v0.1 横幅（#US UTF-16 双 parity 堆）全部命中。
- Burst 编译：`StripPedestrianOverlapsJob` 三平台原生编译成功（job 内没有托管对象引用，Burst 兼容性由编译器背书）。

### 未验证（T4 实机清单，下次会话照此执行）

1. **归档日志**：启动游戏前把 `<UserData>\Logs\Modding.log` 留意干净（BLG §7 T4 协议）。
2. 模组列表出现 Access Anarchy 且无「检测到另一个版本」（本机只有这一份本地模组，无订阅版，无冲突面）。
3. 日志键：`Access Anarchy v0.1 loading` → `AccessZoneOverlapSystem first pass: N candidate lanes`（N 应为正数且远小于全城车道数）→ 心跳行。
4. **功能验收**：找一个停车场/商场出入口，让行人横穿：
   - 车辆不停顿不减速直接穿过行人（需求 4.2.1/4.2.2）；
   - 行人照常行走（4.2.3）；
   - 同一条路人行横道上（远离出入口）车辆仍让行人（4.2.4）——**这是"仅出入口区域"默认模式的关键验收项**；
   - 车辆之间仍正常避让（4.2.5 的车车部分）。
5. 选项页正常渲染（中英标签）、总开关关闭后避让恢复、全局模式下车辆在任何道路都不让行人。
6. 性能：Profiler 中 `AccessZoneOverlapSystem` 每 4 帧一次、应为亚毫秒级。

### 已知限制（v0.1，按影响排序）

1. **实机未验证**（上面 1-6 全部）。最大风险点：出入口判定的覆盖面——若某些建筑的出入口车道不带预期的组件组合（例如某些 mod 资产），会漏删（表现为个别出入口仍让行人）。心跳日志的候选车道数可辅助诊断。
2. 下拉框的两个选项名硬编码英文（框架对 int dropdown 无按值本地化 API，`GetEnumValueLocaleID` 只收 enum）；选项描述已中英双语。后续可自定义枚举类型解决。
3. `MasterLane`/`SlaveLane`（对向车道共享）的 overlap 边界情况未深究——删除只发生在读侧（车辆当前车道），语义上对称安全，但未实测。
4. 行人真的走上车辆车道本体（乱穿马路进入行车道）时的避让走 `CheckCurrentLane` 通用路径（CheckPedestrian 之外），本模组不拦截——极罕见场景，视为保留行为。
5. LaneOverlapSystem 偶发重建（车道 Updated）后，最多 4 帧内出入口车辆短暂恢复避让，然后被本系统再次删除。视觉上不可感知。

---

## 4. 后续方向（按价值排序）

1. **T4 实机验证**（§3 清单），不通过先修判定覆盖面再谈别的。
2. 若实机发现漏判出入口：加"动态学习"模式——统计 `Blocker` 组件为行人的高发车道（车辆被行人堵过的记录在 `Game.Vehicles.Blocker`），自动扩充覆盖。
3. 自定义 `AccessMode` 枚举替换 int dropdown，吃到框架本地化。
4. 白名单 UI（按建筑开关）：需要每建筑实体的覆盖标记组件 + 判定处查询，成本中等。
5. Paradox Mods 发布：`Properties/PublishConfiguration.xml` 已就位，`ModId` 留空，首发用 `PublishNewMod`（BLG §8 硬规则：首发无 dry-run；ModId 回填后一律 NewVersion 传二进制）。

## 5. 本文件夹清单

```
D:\WorkSpace\AccessAnarchy\
├── AccessAnarchy.csproj            工程文件（含构建坑修复注释）
├── AccessAnarchyMod.cs             IMod 入口
├── Setting.cs                      选项面板 + LocaleEN/LocaleZH
├── Systems\AccessZoneOverlapSystem.cs   核心系统（注释含完整机制链）
├── Properties\PublishConfiguration.xml  发布元数据（ModId 158842，与线上同步）
├── README.md                       用户视角说明
├── 开发笔记.md                      ← 本文件
├── research\                       反编译调研产物（csproj 排除编译）
│   ├── decompiled\                 Game.dll 全量反编译（2333 文件，不完整）
│   ├── Game.Simulation.CarLaneSpeedIterator.cs 等  ← -t 单独反编译的关键类型
│   └── gametypes.txt / gamestructs.txt             ← 类型清单
└── template\                       官方 ModTemplate nupkg 解包副本
```
