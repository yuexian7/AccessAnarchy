# Access Anarchy 开发笔记（调研结论 + 实现记录）

- **生成时间**：2026-09-12（v0.6 修订）
- **游戏版本**：1.6.0f1（Unity 2022.3.71f1 构建，工具链 Entities 1.3.10）
- **调研方法**：`ilspycmd`（9.0.0.7889）反编译 `F:\SteamLibrary\steamapps\common\Cities Skylines II\Cities2_Data\Managed\Game.dll`，全文反编译副本在 `research/decompiled/`（2333 个文件，个别复杂类型反编译失败缺失，用 `-t <全名>` 单独反编译补齐；`research/search.mjs` 是源码搜索小工具——注意 findstr 的 `\|` 不是交替语法、`<>` 会被 cmd 重定向吞掉，用 node 搜）。
- **自足性**：新会话只读本文件 + 源码即可继续迭代。所有结论都给了反编译证据位置，查无实据的写成「未验证」。

---

## 0. v0.6 修订（UI 文案：模组名定稿 + 下拉框语言跟随，先读这节）

**用户反馈（功能已验收通过）**：① 模组名统一纯英文 `Access Anarchy`（Anarchy 不好翻译，zh-HANS/HANT 的括号后缀删除）；② 作用范围下拉框只显示简中，切语言不变。

**②的根因（框架机制，反编译核实）**：`DropdownField.Update()`（decompiled/Game/UI/Widgets/DropdownField.cs L62-74）只在 `itemsVersion` 变化时才重新调 `GetModeItems`；`itemsVersion` 来自 `SettingsUIValueVersionAttribute`（AutomaticSettings L510-518, L1268）——**没有该特性的字段版本恒为 0，items 建页时取一次就永不刷新**。GetModeItems 里"实时读语言"的代码本身没错，只是框架根本不再调用它。

**修复**：`Mode` 属性加 `[SettingsUIValueVersion(typeof(Setting), nameof(GetModeItemsVersion))]`，`GetModeItemsVersion()` 返回 `activeLocaleId.GetHashCode()`——语言切换 → 版本变化 → 框架重取 items。这是比 BLG 交接文档 §56/58「控件值建页时抓走」更精确的同族机制：**下拉框 items 的刷新必须显式给版本信号**。

**v0.6 已核验**：`GetModeItemsVersion`、`SettingsUIValueVersion` 均进 DLL；中文括号字样（`出入口无政府）`）已从 DLL 消失。

## 0b. v0.5 修订（横道误伤修复）

**实机反馈**：v0.4 出入口穿透达成，但**人行横道也不避让了**。根因：v0.4 的 A/C 干预点按「距锚点 32m」判定，而出入口与横道在城市里天然紧邻——横道（crosswalk）本身就是 PedestrianLane，被 C 摘了行人注册、被 A 删了引道车道的 overlap 条目。

**修复（v0.5）**：横道在数据层有精确标志——`PedestrianLaneFlags.Crosswalk`（HumanNavigationSystem.GetTargetSpeed 与 SecondaryLaneSystem 都用它区分横道）。
- A（overlap 删除）：对端行人道带 Crosswalk flag → 条目保留（车辆继续给横道让行）；行人连接道条目照删（ConnectionLaneFlags 没有 Crosswalk 概念）。
- C（行人注册删除）：带 Crosswalk flag 的 PedestrianLane 整条跳过。

**方案取舍记录**（用户问「做出入口无碰撞 vs 让行人避让车辆」）：选择继续「出入口无碰撞」+横道精确排除，理由：
1. 需求 2.1.3 明确「行人行为不受影响」，非目标范围明确保留横道避让——横道误伤是 bug 不是 trade-off；
2. 「行人避让车辆」在 CS2 里不是恢复某个被关掉的行为——行人 AI（HumanNavigationSystem 的 UpdateNavigationTarget/GetTargetSpeed，Burst）本来就不避让移动车辆，只对信号灯（LaneSignal.Stop/SafeStop → 行人停）、静态障碍、其他行人反应。要实现它要么重写行人 AI（系统替换，几千行 Burst 逻辑），要么给行人 currentLane 动态塞 LaneSignal——行人连接道是行人动态注册的、川流不息，信号的管理又是新的时序战场，且 Stop 信号会让行人无限堆积在 driveway 前，造成比车等人更糟的新堵塞点；
3. Anarchy 哲学：数据编辑最小化。Crosswalk 排除只是两处 flag 检查。

**v0.5 待实机验证**：出入口穿透保持 + 横道避让恢复。若「横道恢复但出入口失效」，说明测试场景的出入口与横道重叠（生效路径依赖横道数据），需要用户描述具体测试场景再调（候选：横道半径排除改为距离加权、或仅在行人连接道范围内生效）。

## 0b. v0.4 修订（实机日志驱动的空间判定）

**实机日志决定性数据**（`Logs\AccessAnarchy.AccessAnarchyMod.log`——注意 Colossal LogManager 的 logger 各自写 `Logs\<logger名>.log`，Modding.log 只有框架行）：

```
AccessZoneOverlapSystem heartbeat: pass N, 4836 overlap lanes / 0 registration lanes, mode=access-zones
```

**`0 registration lanes` 证明**：全城的连接道（ConnectionLane/GarageLane/AreaLane）**没有任何 LaneObject buffer**——连接道 prefab 资产只有 `Game.Prefabs.ConnectionLane` 组件（其 `GetArchetypeComponents` 只贡献 `Game.Net.ConnectionLane`，反编译核实），没有 CarLane/PedestrianLane 组件（那两个组件类才贡献 LaneObject+LaneOverlap buffer）。由此闭环：

1. 连接道没有 LaneObject buffer → 行人不会注册在连接道上（CheckChanges 走搜索树分支）；
2. 连接道没有 CarLane 组件 → `CarLaneSpeedIterator` 对它完全不检查；
3. 连接道没有 LaneOverlap buffer → 无 overlap 可删；
4. **连接道自身不存在任何可干预数据**——v0.2/v0.3 在连接道层面的干预全部无效。

**实机观察的"出入口避让" therefore 发生在【出入口紧邻的普通马路车道（引道）与行人道的 LaneOverlap】上**：行人横穿入口处的人行道/横道（注册在人行道上），车辆在引道马路车道上经 overlap 读到行人减速。这类车道是普通 CarLane，任何"按车道类型"的判定都覆盖不到——全局模式生效（全删）与此完全自洽。

**v0.4 解法 = 空间判定（锚点网格）**：
- `BuildAnchorGridJob`（每 512 帧，单线程）：遍历连接道/GarageLane/AreaLane，取 Curve 五个控制点写入 `NativeHashMap<int2, FixedList128Bytes<float2>>`（32m 网格）。
- 判定 `IsNearAnchor`：车道的 a/d/中点查 3×3 邻域格 + 32m 距离校验。
- **A 扩展**（每 4 帧）：邻近锚点的普通车辆车道 → 删其 LaneOverlap 的行人条目（覆盖引道↔人行道/横道）。受 IncludeConnections 开关控制。
- **C 新增**（每帧）：`StripPedestrianOnNearbyWalkwaysJob`——邻近锚点的 PedestrianLane → 删其 LaneObjects 里的行人与动物。横穿出入口区域的行人无论注册在哪条行人道上都被摘除（釜底抽薪）。全局模式下处理全城行人道。
- **B 保留**（每帧）：连接道类兜底（实测 0 条，防 mod 资产引入带 buffer 的连接道）。
- 行人带 `Game.Creatures.Creature` 组件已确认（`Game.Creatures.InitializeSystem` 查询 `ReadOnly<Creature>()` 同时处理 Human/Animal）。
- **ECB 回放滞后一帧的时序**（v0.3 教训）：删除命令帧末回放、CarNav 帧中读取 → CarNav 每帧读到的是"上一帧末删除后"的状态 → B/C 必须每帧执行，缺席任何一帧车辆就会看到重加的行人。

**v0.4 待实机验证**：锚点网格是否覆盖用户测试场景。若仍有个别出入口不生效，把日志 heartbeat 里的 anchors 数量与具体位置发来；半径 32m（`kAccessRadius`）可调。

#### 0.1.1 本地化：12 份字典全被填成同一语言（v0.3）

**症状**：游戏切英文仍显示中文。**根因**：`LocaleTable.Build(setting, locale)` 内部用 `T(key)` 取词，而 `T()` 读的是全局静态 `_activeLocale`（OnLoad 时被设为当时的游戏语言），与传入的 `locale` 参数无关——于是 12 份 `AddSource` 字典全部填成了同一语言。**修复**：`Build` 改为按传入 locale 取各自词条表（`d["..."]`）。教训：**AddSource 的每份字典必须各自构建，任何"当前语言"状态只允许用于 dropdown displayName 这类运行时取值**。dropdown 选项名由 `GetModeItems()` 实时读 `GameManager.instance.localizationManager.activeLocaleId`（只在建页/刷新时调用，非热路径）。

### 0.2 "仅出入口"模式失效：B 干预点的删除节奏跟不上重加

**根因是 ECB 回放滞后一帧 + 删除间隔 4 帧**。精确时序（v0.2，每 4 帧）：

```
帧 0:  job 读 buffer（= 帧-1末回放状态，行人条目在）→ 记录 SetBuffer → CarNav 读（脏，减速）
       → 帧末回放（删除生效）
帧 1:  不跑 → CarNav 读（干净 ✓）→ HumanNav 切片重加
帧 2:  不跑 → CarNav 读（脏，减速！）
```

4 帧里 3 帧可见 → 表现为"完全没生效"。**修复**：B 改为**每帧**执行——每帧都记录删除命令，CarNav 每帧读到的都是"上一帧末已删除"的状态，行人永远不可见。同时把 B 的查询从全城车辆车道收窄为 `Any=[GarageLane, ConnectionLane, AreaLane]`（出入口核心车道，量小，每帧成本可忽略）；普通道路车道继续由 A 管（overlap 删除的重加来源是 LaneOverlapSystem 的车道重建，频率低，每 4 帧够用——全局模式实测有效）。

**顺带确认**：行人实体确实带 `Game.Creatures.Creature` 组件（`Game.Creatures.InitializeSystem` 的 `m_CreatureQuery = GetEntityQuery(ReadOnly<Creature>(), ReadOnly<Updated>())` 同时处理 Human/Animal），B 的删除判据成立。

**残余风险（下一轮实机验证点）**：若每帧删除后仍有出入口避让，下一个嫌疑是"行人横穿 driveway 时的 currentLane 是长段人行道而非行人连接道"（B 刻意不删人行道注册以免破坏马路避让），届时改按建筑包围盒做空间判定。

---

## 0b. v0.2 修订（第一轮实机反馈驱动的机制修正）

**实机结论**：全局模式生效（马路上的车不让人了），"仅出入口"模式完全无效。由此反推出 v0.1 调研的一处关键错误并已修正：

1. **连接道（driveway/garage）根本不存在 LaneOverlap 条目可删**。证据链：
   - 所有建筑连接道都带 `SecondaryLane` 组件（`Game.Buildings.RoadConnectionSystem.cs` 1334/1438 行无条件 AddComponent）；
   - `LaneOverlapSystem.UpdateOverlaps` 对 `m_SecondaryLaneData.HasComponent(subLane)` 的车道直接跳过（写入侧排除，decompiled/Game/Net/LaneOverlapSystem.cs 1043 行），`CheckOverlaps` 的对端也排除（1116 行）；
   - LaneOverlap buffer 只来自 archetype：`Game.Prefabs.CarLane`/`Game.Prefabs.PedestrianLane` 组件的 `GetArchetypeComponents` 贡献 `LaneObject`+`LaneOverlap`（全树无 `AddBuffer<LaneOverlap>`，唯二来源）；而 `Game.Prefabs.ConnectionLane` 组件只贡献 `Game.Net.ConnectionLane`——**连接道的 overlap buffer 永远是空的**。
   - 所以 v0.1 的"删除连接道 overlap"分支是 no-op，`IncludeConnections` 按 flags 过滤（Road|Track|Parking）的疑虑反而不是主因。
2. **车辆"看见"行人的唯一公共汇聚点是车道的 `LaneObject` buffer**：`CheckCurrentLane`（本车道）与 `CheckOverlappingLanes`（经 overlap 指向的对端车道）最终都读某个车道的 LaneObjects 找带 `Game.Creatures.Creature` 的对象。行人由 `HumanNavigationSystem.CurrentLaneCache.CheckChanges` 注册到**自己 currentLane** 的 LaneObjects（research/Game.Simulation.HumanNavigationHelpers.cs 54-91 行）。
3. **v0.2 干预点 B**：新增 `StripPedestrianRegistrationsJob`——删除"出入口区域车道"（GarageLane / 任意 ConnectionLane / AreaLane / 建筑自有 CarLane-ParkingLane）LaneObjects 里的行人与动物条目。无论车辆通过哪条路径读行人注册，读的都是这份 buffer——釜底抽薪。原 overlap 删除（干预点 A）保留，它管马路上的行人横道场景（实机已验证有效）。
4. **`GarageLane` 组件只在创建 Parking 连接道时由 RoadConnectionSystem 附加**（1439-1442 行），是连接道的可靠标记之一。
5. AreaLane 内部（`CarCurrentLane.m_LaneFlags & Game.Vehicles.CarLaneFlags.Area`）车辆本就只对目标点减速、不查行人（CarNavigationSystem 1573/1910-1958 行：Area 分支用 VehicleCollisionIterator 且它 skip Creature）——停车场内部天然穿透，与 v0.1 结论一致。

v0.2 已知残余风险（待实机复验）：若行人横穿 driveway 时的 currentLane 既不是行人连接道也不是出入口车道（例如长段人行道横跨整个入口区），B 不会删它在人行道上的注册（刻意不删，避免破坏马路避让）——此时该行人仍会让车停。实机若再现，考虑按"建筑包围盒邻近"做空间判定。

---

## 1. 核心调研结论：车辆避让行人的机制链

### 1.1 唯一动态机制 = CarLaneSpeedIterator.CheckPedestrian

`Game.Simulation.CarNavigationSystem.UpdateNavigationJob`（Burst 编译，每帧对 1/16 切片的车辆运行）调用 `Game.Simulation.CarLaneSpeedIterator`（struct，反编译件 `research/Game.Simulation.CarLaneSpeedIterator.cs`）：

1. `IterateFirstLane(...)`（118 行起）：对车辆当前车道，若是 `CarLane`（196 行起还有 `PedestrianLane` 分支），调用
2. `CheckCurrentLane(...)`（1103 行）：遍历**本车道** `LaneObjects` 缓冲（同车道对象，行人乱穿马路时走到这里）；
3. `CheckOverlappingLanes(...)`（1172 行）：遍历本车道 `LaneOverlap` 缓冲，对每条 overlap 把 `m_Lane` 切到对端车道，读**对端车道的 LaneObjects**（1324 行起）：
   - 对端车道对象带 `Game.Creatures.Creature` 组件 → `CheckPedestrian(...)`（1346-1348 → 1403-1431 行）→ 按刹车距离压低 `m_MaxSpeed`，`m_BlockerType = Temporary`。**行人在车辆的曲线区间内时无条件减速（第一个 if 不成立直接落到底部），没有任何数据开关可以绕过。**
   - 不带 `Creature` → 走通用 `UpdateMaxSpeed`（车/静物避让）。

行人是怎么被看到的：行人行走时由 `Game.Simulation.HumanNavigationSystem`（每帧 1/16 切片）的 `HumanNavigationHelpers.CurrentLaneCache.CheckChanges` 把行人**注册到自己所在车道的 `LaneObject` 缓冲**（反编译件 `research/Game.Simulation.HumanNavigationHelpers.cs` 54-91 行：`buffer.Add(currentLane.m_Lane, entity, curvePos)`；不在车道上时注册进移动对象四叉树）。行人**永远不会被注册到车辆车道上**——车辆车道能"看到"行人，完全靠 `LaneOverlap` 条目指向行人道这一层引用。

> **结论 A（干预点）**：删除"车辆车道 ↔ 行人道"的 `LaneOverlap` 条目 = 车辆彻底看不到行人。这是唯一充分且最小的数据层干预点。
>
> **结论 B（Harmony 不可行）**：CheckPedestrian 在 Burst 编译的 job 内被内联，Harmony 无法拦截；CarNavigationSystem 约 3000 行逻辑，系统替换不现实。
>
> **结论 C（AreaLane 内已天然穿透）**：`Game.Simulation.VehicleCollisionIterator.CheckCollision`（research/Game.Simulation.VehicleCollisionIterator.cs 204-209 行）对带 `Creature` 组件的移动对象直接 `return`——只在 AreaLane（停车场区域车道）上迭代的它本来就跳过行人。

### 1.2 数据结构与重建时机

- `LaneOverlap`（`Game.Net.LaneOverlap`，buffer 挂在车道实体上，`[InternalBufferCapacity(0)]`）：`m_Other`（对端车道）、`m_Flags`、曲线区间字节、`m_PriorityDelta`。**双向记录**（A 有指向 B 的条目，B 也有指向 A 的），但车辆只读自己一侧的缓冲——只删车辆车道侧即可，不动行人道侧。
- `LaneObject`（行人/车辆的登记条目，含曲线位置）由各导航系统自行维护，与本模组无关。
- **重建时机**（`Game.Net.LaneOverlapSystem`，反编译件 research/decompiled/Game/Net/LaneOverlapSystem.cs 1893-1990 行）：世界加载后第一轮全量重建（`m_AllLanesQuery`）、车道带 `Updated` 事件时局部重建（`m_UpdatedLanesQuery`）。`UpdateLaneOverlapsJob` 会整体重写目标车道的 overlap 缓冲 → 本模组的删除会被冲掉 → **必须周期性重新应用**。
- overlap 缓冲**不序列化**（LaneOverlap 结构无 Serialize），存档重载后由 LaneOverlapSystem 重建，天然与本模组无持久化冲突。
- `Game.Net.FixLaneObjectsSystem` / `Game.Serialization.LaneObjectSystem` 只修 `LaneObject`，不碰 overlap。

### 1.3 碰撞事件与撞击

- `Game.Events.Impact` 组件的创建者只有 `Game.Events.InitializeSystem.InitializeTrafficAccident`（随机交通事故事件）和 `Game.Simulation.ObjectCollisionSystem`（只处理 `OutOfControl` 的失控车）。**正常车辆与行人模型重叠不产生任何撞击事件**——车辆模型穿过行人时没有物理碰撞、没有动画、没有事件。需求 4.2.2（无碰撞动画/事件）在删除减速逻辑后天然满足。
- `Game.Objects.LaneBlockSystem` 只处理带 `BlockedLane` 的静态物（垃圾桶等），与行人无关。
- `Game.Rendering.CameraCollisionSystem` 只管相机。

### 1.4 时序（Game.Common.SystemOrder.cs，反编译件 research/decompiled/Game/Common/ 下）

```
SystemUpdatePhase.GameSimulation:
  310: CarNavigationSystem            ← 读 LaneOverlap 决定避让
  311: CarNavigationSystem.Actions    ← 车辆自己的 LaneObject 注册
  ...
  343: HumanNavigationSystem          ← 行人的 LaneObject 注册（在 CarNav 之后！）
```

本模组系统注册为 `UpdateAt<AccessZoneOverlapSystem>(GameSimulation)` + `UpdateBefore<AccessZoneOverlapSystem, CarNavigationSystem>(GameSimulation)`。每 4 个模拟帧跑一次：删除（EndFrameBarrier 帧末回放）→ CarNav 读到无行人 overlap → HumanNav 下一帧的重新注册**不产生车辆车道侧条目**（见 1.1，行人只注册到自己的人行道）→ LaneOverlapSystem 偶尔重建的条目最多存活 4 帧。

### 1.5 出入口车道的判定（AccessZoneOverlapSystem.IsTargetLane）

| 类别 | 判定 | 反编译依据 |
|---|---|---|
| 车库坡道 | 车道实体带 `Game.Net.GarageLane` | 停车楼/车库出入口坡道 |
| 建筑出入连接道 | 带 `Game.Net.ConnectionLane` 且 `m_Flags & (Road\|Track\|Parking) != 0` | ConnectionLaneFlags（Road=0x20, Track=0x40, Parking=0x100）；行人连接道只有 Pedestrian=0x80 |
| 停车场内部行驶道 | 带 `Game.Net.AreaLane` | 挂在 Area 实体上的区域车道；`Game.Vehicles.CarLaneFlags.Area`（0x4000000）是**车辆 CarCurrentLane 的状态标志**，不在车道组件上（第一个坑，见 §3） |
| 建筑自有车道/停车位 | 带 `CarLane` 或 `ParkingLane`，且 `Owner` 链上所有者**不是** `Game.Net.Edge`/`Game.Net.Node`（即不是道路自有 → 不是路边停车位） | Owner（Game.Common.Owner，m_Owner 单跳） |
| 全局模式 | 上述全部（纯行人连接道跳过） | |

"对端是行人道"判定：`PedestrianLane` 组件存在，或（双保险）`ConnectionLane` 带 `Pedestrian` 标志且无车辆标志。

### 1.6 行人侧不读 overlap 的验证

行人行走（HumanNavigationSystem）与行人寻路（Game.Pathfind）均不读取 `LaneOverlap`；行人等信号灯走 `LaneSignal`（`GetTargetSpeed`，research/Game.Simulation.HumanNavigationSystem.cs 696-703 行）。删除车辆车道侧 overlap 对行人零影响。

---

## 2. 实现记录

- **零 Harmony**。csproj 不引用 Lib.Harmony（对比 BridgeTheLanguageGap 的发布产物少一个 0Harmony.dll）。
- 系统：`Systems/AccessZoneOverlapSystem.cs`（partial，源生成器要求）。Job `StripPedestrianOverlapsJob`（IJobChunk，Burst 编译成功——构建日志可见三平台 Burst 原生库生成）。
- **ECB 没有单条删除 buffer 元素的命令**（Entities 1.3 的 `EntityCommandBuffer.ParallelWriter` 无 RemoveBufferElement），用 `ecb.SetBuffer<LaneOverlap>(sortKey, entity)` 原子替换整个缓冲：保留全部非行人条目（车车 overlap 与优先级数据不动），行人对条目消失。记录前先 `HasPedestrianOverlap` 预检，无行人条目时零写入（幂等，避免每帧无谓的结构性变更）。
- 查询：`All=[LaneOverlap], Any=[CarLane, ParkingLane, GarageLane, ConnectionLane, AreaLane], None=[Deleted, Temp]`，`RequireForUpdate`。
- 选项：官方 ModSetting 框架（`[FileLocation]` + `AssetDatabase.global.LoadSettings` 落盘到 `ModsSettings\AccessAnarchy.coc`）；系统通过 `Setting.Instance` 静态引用在调度 Job 前读 getter（框架在主线程直调 setter，getter 恒为当前生效值，无需静态镜像——对比 BLG 交接文档 §10 #9 的场景：本模组读设置在主线程 OnUpdate 内，不是热路径）。
- 日志：首跑 + 每 1800 帧心跳（候选车道数、当前模式），日志键 `AccessZoneOverlapSystem`。
- 构建坑（与 BLG 交接文档 §10 一致的全部踩过）：
  - csproj 必须显式 `<Compile Remove="research\**;template\**">`，否则 2333 个反编译文件进编译（T3 教训的镜像）。
  - `RunModPostProcessor`/`RunModPublisher` 重声明 + `DOTNET_ROLL_FORWARD=Major` + `BeforeTargets="DeployWIP"`（§10 #23）。
  - IJobChunk 的 Execute 只有一个四参数签名（`in v128` 来自 `Unity.Burst.Intrinsics`），系统类必须 `partial`。
  - `Game.SystemUpdatePhase.GameSimulation`（枚举在根命名空间，不是 Game.Common）。
  - `EndFrameBarrier` 在根命名空间 `Game`。

---

## 3. 验证状态（诚实台账）

### 已验证（机器证据）

- T1 编译门禁：0 警告 0 错误，`AccessAnarchy.dll processing complete`，部署到 `<UserData>\Mods\AccessAnarchy\`（6 文件：托管 dll/pdb + 三平台 Burst 原生库）。
- T2 静态核验（node 双堆扫描，方法同 BLG verify.mjs）：类型 `AccessZoneOverlapSystem`/`StripPedestrianOverlapsJob`（#Strings UTF-8 堆）、首跑日志/心跳/v0.1 横幅（#US UTF-16 双 parity 堆）全部命中。
- Burst 编译：`StripPedestrianOverlapsJob` 三平台原生编译成功（job 内没有托管对象引用，Burst 兼容性由编译器背书）。

### 未验证（T4 实机清单，下次会话照此执行）

1. **归档日志**：启动游戏前把 `<UserData>\Logs\Modding.log` 留意干净（BLG §7 T4 协议）。
2. 模组列表出现 Access Anarchy 且无「检测到另一个版本」（本机只有这一份本地模组，无订阅版，无冲突面）。
3. 日志键：`Access Anarchy v0.1 loading` → `AccessZoneOverlapSystem first pass: N candidate lanes`（N 应为正数且远小于全城车道数）→ 心跳行。
4. **功能验收**：找一个停车场/商场出入口，让行人横穿：
   - 车辆不停顿不减速直接穿过行人（需求 4.2.1/4.2.2）；
   - 行人照常行走（4.2.3）；
   - 同一条路人行横道上（远离出入口）车辆仍让行人（4.2.4）——**这是"仅出入口区域"默认模式的关键验收项**；
   - 车辆之间仍正常避让（4.2.5 的车车部分）。
5. 选项页正常渲染（中英标签）、总开关关闭后避让恢复、全局模式下车辆在任何道路都不让行人。
6. 性能：Profiler 中 `AccessZoneOverlapSystem` 每 4 帧一次、应为亚毫秒级。

### 已知限制（v0.1，按影响排序）

1. **实机未验证**（上面 1-6 全部）。最大风险点：出入口判定的覆盖面——若某些建筑的出入口车道不带预期的组件组合（例如某些 mod 资产），会漏删（表现为个别出入口仍让行人）。心跳日志的候选车道数可辅助诊断。
2. 下拉框的两个选项名硬编码英文（框架对 int dropdown 无按值本地化 API，`GetEnumValueLocaleID` 只收 enum）；选项描述已中英双语。后续可自定义枚举类型解决。
3. `MasterLane`/`SlaveLane`（对向车道共享）的 overlap 边界情况未深究——删除只发生在读侧（车辆当前车道），语义上对称安全，但未实测。
4. 行人真的走上车辆车道本体（乱穿马路进入行车道）时的避让走 `CheckCurrentLane` 通用路径（CheckPedestrian 之外），本模组不拦截——极罕见场景，视为保留行为。
5. LaneOverlapSystem 偶发重建（车道 Updated）后，最多 4 帧内出入口车辆短暂恢复避让，然后被本系统再次删除。视觉上不可感知。

---

## 4. 后续方向（按价值排序）

1. **T4 实机验证**（§3 清单），不通过先修判定覆盖面再谈别的。
2. 若实机发现漏判出入口：加"动态学习"模式——统计 `Blocker` 组件为行人的高发车道（车辆被行人堵过的记录在 `Game.Vehicles.Blocker`），自动扩充覆盖。
3. 自定义 `AccessMode` 枚举替换 int dropdown，吃到框架本地化。
4. 白名单 UI（按建筑开关）：需要每建筑实体的覆盖标记组件 + 判定处查询，成本中等。
5. Paradox Mods 发布：`Properties/PublishConfiguration.xml` 已就位，`ModId` 留空，首发用 `PublishNewMod`（BLG §8 硬规则：首发无 dry-run；ModId 回填后一律 NewVersion 传二进制）。

## 5. 本文件夹清单

```
D:\WorkSpace\AccessAnarchy\
├── AccessAnarchy.csproj            工程文件（含构建坑修复注释）
├── AccessAnarchyMod.cs             IMod 入口
├── Setting.cs                      选项面板 + LocaleEN/LocaleZH
├── Systems\AccessZoneOverlapSystem.cs   核心系统（注释含完整机制链）
├── Properties\PublishConfiguration.xml  发布元数据（ModId 空）
├── README.md                       用户视角说明
├── 开发笔记.md                      ← 本文件
├── research\                       反编译调研产物（csproj 排除编译）
│   ├── decompiled\                 Game.dll 全量反编译（2333 文件，不完整）
│   ├── Game.Simulation.CarLaneSpeedIterator.cs 等  ← -t 单独反编译的关键类型
│   └── gametypes.txt / gamestructs.txt             ← 类型清单
└── template\                       官方 ModTemplate nupkg 解包副本
```
