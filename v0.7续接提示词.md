# v0.7.0 续接提示词（AccessAnarchy）— 生成于本会话 native compaction 前

## 任务（两个需求）
1. **优化帧数**：57km×57km 超大图帧数减半；检测机制随城市扩大变卡。
2. **设置界面加 2 个快捷键**：
   - 启用/关闭 = `Enabled` 总开关
   - 切换避让范围 = 循环 `Auto(默认避让全部) -> BuildingOnly -> RoadOnly -> None -> 回 Auto`

## 磁盘真实状态（刚核实）
- HEAD=6954e62 (v0.6.0)，工作树干净（仅 untracked: `out_api.txt`, `tools/`）。
- 代码文件：`AccessAnarchyMod.cs`(66行)、`Setting.cs`(544行)、`Systems/AccessZoneOverlapSystem.cs`(741行)、`Properties/AssemblyInfo.cs`。
- **注意**：v0.7 的设计文档**未写入磁盘**（上一轮会话因上下文超限被系统原生压缩，`v0.7的设计文档.md` 随上下文一起丢失，需重写或由旧会话找回）。
- 上一轮会话已因上下文超限触发 native compaction，历史工具输出不可恢复。

## 性能方案（需求1，已设计未实施）
现状根因（`Systems/AccessZoneOverlapSystem.cs`）：
- `OnGameViewInput`（UI 事件）里 `FindObjectsByType<NetInfo>` 每帧全堆扫描（~10ms+/帧）。
- `Render` 每帧 O(选中×分区×道路) 扫全城网格。
- `m_lastHash` 含 `cameraPosition` 且绘制块被注释 → 缓存失效却仍全量重绘。

修法：
1. `OnCreate` 用 `ResourceManager.GetOrLoad<NetInfo>` + 订阅 `NetInfo.eventOnResourcesReleased` 维护静态 `s_netInfos`，删每帧扫描。
2. `Render` 入口 `if (cameraPosition == m_lastCam) return;`。
3. `OnEnd` destroying 守卫。
4. `UpdateDebugState` 节流 ~0.5s。
5. `Render` 加 500ms/50万单元预算护栏。
6. `DrawRoadOverlay` 用 `lane.m_laneWidth` 提常数。

## 快捷键方案（需求2，已设计未实施；API 曾核实，续接会话**必须重新 grep 验证**后才能写码）
- 反编译源：`research/decompiled/`（顶层有 `ModSetting.cs`，`Game/Modding/` 亦有；参考实现 `Game/Modding/KeybindingSettings*` 路径需重新确认）。
- 核心链（上轮结论，待重验）：
  - `ModSetting.SetBinding` → `Binding.Create(action, "<Keyboard>/key")` + `ProxyAction.Register(action.name, "gameactions", ...)`（动作必须挂 `gameactions` map 才被 `InputActionAsset.actions` 收录，且须先于首次 `InputManager.InstantiateForRuntime`）。
  - 绑定持久化靠 `settings.Register(path, binding)`（`proxy.m_Path == settings path`）。
  - 触发：`InputManager.onBeforeUpdate` → `proxy.GetBinding()?.WasPressedThisFrame()`。
  - 执行：`InputManager.PerformAction(ref binding, context=null, bindOptions)`（player-mapped 会写 CurrentContext）。
  - 重置：`proxy.RemoveBindings()` + `GetBinding()` 断旧回调 + `binding.Dispose()`。
- UI：`Game/Input/UI` 有 `Button` 控件；Setting.cs 里已有可编译模板：`ButtonActionListener`（`OnCreate` 里 `new Button{ m_OnClick=listener }` / `RemoveAt` 用法）；文本模板 `Setting.cs:431` `Text{ isRichText, m_Text }`。
- 设置页：`Setting.cs` `CreateSettingsPanel` 布局为 `ScrollView` + `GUILayout.BeginVertical` 手写，面板末尾加面板即可。
- Mod 默认键：**F3=Enabled、F4=切换避让**（用户已选）。
- 冲突须 UI 显式报错（不自动抢键）；避让模式枚举化存旧 bool（`IsBuilding` 位0 / `IsRoad` 位1）。

## 流程约束（用户定，必须遵守）
- **任何代码修改前**：bash 原始 grep + read 盘上原文，先列「当前原文(路径:行号)」与「将要改成」的精确 diff 给用户确认；**禁止凭记忆/总结写码或下 API 结论**（上轮多次因此返工）。
- 写码后 `dotnet build`（`-p:CitiesGameLibsDir` 见 `开发笔记.md` 发布节）+ 反编译产物校验 IL + P0 自查；版本号更新与发布记录放最后确认。
- bash 在 WSL：仓库路径 `/mnt/d/WorkSpace/AccessAnarchy`；勿用 read 工具读 `research/decompiled` 超大文件（用 bash grep/sed）。
